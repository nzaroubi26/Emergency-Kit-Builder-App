# Story 15.2: Visualizer Layout Refresh — Vertical to Horizontal

## Status: Draft

## Story

**As a** user,
**I want** the kit builder screen to display the housing unit and subkit options side-by-side,
**so that** I can see my selections and available options simultaneously without scrolling.

## Acceptance Criteria

1. SubkitSelectionScreen layout is updated: housing unit visualizer on the left (40%), subkit card list on the right (60%) on desktop (≥1024px). Stacked on tablet/mobile (visualizer top, cards below).
2. The housing unit visualizer component (`HousingUnitVisualizer`) is unchanged internally. Its `max-w-sm` (384px) fits within the 40% column at 1024px. No interface changes needed.
3. SubkitCards are displayed in a single-column scrollable list within the right panel (desktop) or a 2-column grid (tablet/mobile), per Sally's Sprint 2 spec.
4. Slot usage indicator (`SlotUsageBar`), `SlotFullIndicator` warning, and "Configure Items" CTA are pinned in the left column below the visualizer (desktop). On stacked mobile, they sit between the visualizer and the card list.
5. All existing functionality is preserved: select/deselect, size toggle, slot count impact, capacity enforcement, "Configure Items" navigation.
6. Category color updates (if any) are applied to `CATEGORIES` `colorBase`/`colorTint` values per Sally's Sprint 2 spec. *(Sally Decision #5: no existing color changes — Sprint 2 color work is additive only via Pets.)*
7. Left column is sticky on desktop (`position: sticky; top: 1.5rem`) so the visualizer stays visible while scrolling the card list.
8. Component tests updated for new layout assertions. All existing behavioral tests still pass.

## Tasks / Subtasks

- [ ] Task 1: Restructure SubkitSelectionScreen layout (AC: 1, 2, 4, 7)
  - [ ] Wrap existing content in a flex container: `flex gap-8` on desktop, `flex-col gap-6` below 1024px
  - [ ] Left column (`w-[40%]`): HousingUnitVisualizer + SlotUsageBar + SlotFullIndicator + "Configure Items" CTA
  - [ ] Right column (`w-[60%]`): SubkitCard list
  - [ ] Apply `sticky top-6` to left column on desktop
  - [ ] Content container: `max-w-[1120px] mx-auto px-6`
- [ ] Task 2: Update SubkitCard list rendering (AC: 3)
  - [ ] Single-column list on desktop (right panel)
  - [ ] 2-column grid on tablet/mobile (<1024px) with `gap-3`
- [ ] Task 3: Verify all existing functionality (AC: 5)
  - [ ] Select/deselect subkits still works
  - [ ] Size toggle still works
  - [ ] Slot count impact unchanged
  - [ ] Capacity enforcement (6 slots) unchanged
  - [ ] "Configure Items" CTA navigates to correct config screen
- [ ] Task 4: Update tests (AC: 8)
  - [ ] Update layout assertion tests for new horizontal layout
  - [ ] All existing behavioral tests still pass
  - [ ] Run `tsc --noEmit` and `npm run test:run`

## Dev Notes

### Architecture Reference
[Source: docs/architecture-phase3-sprint2.md, Section 2]

`HousingUnitVisualizer` needs no interface changes — its `max-w-sm` (384px) already fits within the 40% column. SlotUsageBar/CTA relocation is JSX-only restructuring within the same component. All elements read from the same hooks they use today.

### Layout Spec
[Source: docs/front-end-spec-phase3-sprint2.md, Section 4]

| Element | Treatment |
|---------|----------|
| Page background | `neutral-50` (`#F8F9FA`) |
| Content container | `max-w-[1120px] mx-auto px-6` |
| Layout grid | `flex gap-8` (32px) on desktop; `flex-col gap-6` below 1024px |
| Left column | `w-[40%]` — sticky at `top-6` |
| Right column | `w-[60%]` — scrollable |
| Back link | `← Back` — routes to `/choose` |
| Page heading | "Build Your Emergency Kit" — `text-h1` (28px/700), `neutral-900`, centered |

### Responsive Breakpoints
[Source: docs/front-end-spec-phase3-sprint2.md, Section 8]

| Property | Desktop (≥1024px) | Tablet (768–1023px) | Mobile (<768px) |
|----------|-------------------|---------------------|-----------------|
| Layout | Side-by-side 40/60 | Stacked | Stacked |
| Card grid | Single-column list | 2-column grid, `gap-3` | 2-column grid, `gap-3` |
| Left column | `w-[40%]`, sticky | Full-width | Full-width |

### Relevant Source Tree
```
src/
  components/
    subkit-selection/
      SubkitSelectionScreen.tsx  # MODIFY — layout restructure
      SubkitCard.tsx             # UNCHANGED (layout adapts via container)
    visualizer/
      HousingUnitVisualizer.tsx  # UNCHANGED — no interface changes
```

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]
- All 14 critical rules apply without exception
- Dynamic category colors: inline `style` prop (existing architectural rule)

### Testing
[Source: docs/architecture/9-testing-requirements.md]
- Update layout assertion tests in existing SubkitSelectionScreen test file
- All behavioral tests must still pass
- Run `tsc --noEmit` and `npm run test:run`

### Integration Verification
- IV1: Visualizer and subkit list display side-by-side on desktop.
- IV2: All existing subkit selection interactions work identically.
- IV3: Capacity system (6 slots, Large = 2 slots) is unchanged.
- IV4: "Configure Items" CTA still navigates to the correct first subkit config screen.
- IV5: Layout stacks correctly on tablet/mobile viewports.

## File List

| File | Action | Description |
|------|--------|-------------|
| src/components/subkit-selection/SubkitSelectionScreen.tsx | MODIFY | Layout restructure: vertical → 40/60 horizontal, sticky left column |

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-14 | 1.0 | Initial story draft | Sarah, PO Agent |

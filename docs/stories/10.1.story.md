# Story 10.1: Housing Unit Visualizer — Outer Frame, Handle Tab, and Wheel Guards

## Status: Ready for Review

## Story

**As a** user,
**I want** the Housing Unit Visualizer to look like the actual physical storage unit,
**so that** I have a clear visual sense of the product I am configuring.

## Acceptance Criteria

1. A new wrapper element is added inside `HousingUnitVisualizer.tsx` surrounding the existing slot grid `div`. The `HousingUnitVisualizer` exported props interface (`slots`, `readOnly`, `onSlotClick`) is unchanged. The `SlotState` data model is unchanged. All existing tests for `HousingUnitVisualizer` pass without modification.

2. The outer frame is implemented as a wrapping `div` with:
   - Background: `neutral-400` (`#9CA3AF`)
   - Border radius: `rounded-xl` (12px)
   - Padding: `p-3` (12px all sides), `pb-2` (8px bottom)
   - The existing slot grid sits inside this frame unchanged.

3. The handle tab is a sibling `div` rendered above the outer frame, visually protruding upward via negative bottom margin overlap:
   - Width: 80px (`w-20`); Height: 24px (`h-6`)
   - Style: Hollow border (`border-neutral-400`, `bg-transparent`) with 6px top border and side borders
   - Border radius: `rounded-t-md` (6px) on top corners only; 0px on bottom corners
   - Horizontally centered (via flex parent or `mx-auto`)
   - Visually appears to protrude upward from the top edge of the outer frame

4. Two wheel guard `div`s are absolutely positioned outside the outer frame:
   - Width: 24px (`w-6`); Height: 48px (`h-12`) each
   - Background: `neutral-600` (`#4B5563`)
   - Border radius: `rounded-sm` (3px all corners, symmetric)
   - Positioning: Absolute positioned outside the frame using negative offsets (`-bottom-3 -left-6` / `-right-6`)

5. The exterior redesign applies to both the interactive variant (on `SubkitSelectionScreen`) and the `readOnly` variant (on `SummaryScreen`). Both variants render the frame, handle tab, and wheel guards identically.

6. All existing slot behaviors are unaffected:
   - Filled slots on `SubkitSelectionScreen` continue to display `cursor-pointer` and `hover:brightness-95` with `onSlotClick` navigation (Phase 2 Story 7.2)
   - Empty slots remain non-interactive with no cursor change
   - `readOnly` mode on `SummaryScreen` remains fully non-interactive
   - Slot fill animations (Phase 1, animations #1–#3, #9) are unaffected

7. The redesign is compatible with the existing visualizer container card on `SubkitSelectionScreen` (white card, `shadow-2`, `radius-lg`, `neutral-200` border, 16px padding). The outer shell sits inside that container card as before.

8. `HousingUnitVisualizer.test.tsx` is updated with:
   - Test confirming outer frame wrapper element is present in the rendered output
   - Test confirming handle tab element is present
   - Test confirming two wheel guard elements are present
   - All existing tests continue to pass

## Tasks / Subtasks

- [x] Task 1: Add outer frame wrapper (AC: 1, 2, 7)
  - [x] Open `src/components/visualizer/HousingUnitVisualizer.tsx`
  - [x] Add a new `div` wrapping the existing slot grid inside the component's root element
  - [x] Style: `bg-neutral-400` (`#9CA3AF`), `rounded-xl` (12px)
  - [x] Padding: `p-3` (12px all sides), `pb-2` (8px bottom)
  - [x] Existing slot grid sits inside unchanged
  - [x] Props interface (`slots`, `readOnly`, `onSlotClick`) remains unchanged
- [x] Task 2: Add handle tab (AC: 3)
  - [x] Add a sibling `div` rendered above the outer frame
  - [x] Dimensions: 80px wide (`w-20`) × 24px tall (`h-6`)
  - [x] Style: Hollow border (`border-neutral-400`, `bg-transparent`) with 6px top border and side borders
  - [x] Border radius: `rounded-t-md` (6px) on top corners only, 0 on bottom
  - [x] Position: horizontally centered (via flex parent or `mx-auto`); visually protrudes upward via negative bottom margin overlap
- [x] Task 3: Add two wheel guards (AC: 4)
  - [x] Add two absolutely positioned `div`s outside the outer frame
  - [x] Each: 24px wide (`w-6`) × 48px tall (`h-12`)
  - [x] Background: `neutral-600` (`#4B5563`)
  - [x] Border radius: `rounded-sm` (3px all corners, symmetric)
  - [x] Position: absolute, outside the frame using negative offsets (`-bottom-3 -left-6` / `-right-6`)
- [x] Task 4: Verify both variants render correctly (AC: 5, 6, 7)
  - [x] Verify `SubkitSelectionScreen` renders the outer shell (interactive variant)
  - [x] Verify `SummaryScreen` renders the outer shell (`readOnly` variant)
  - [x] Verify filled slot click navigates correctly (Phase 2 Story 7.2 unaffected)
  - [x] Verify empty slots remain non-interactive
  - [x] Verify slot fill animations are unaffected
  - [x] Verify compatibility with existing visualizer container card
- [x] Task 5: Update HousingUnitVisualizer tests (AC: 8)
  - [x] Open `tests/components/HousingUnitVisualizer.test.tsx`
  - [x] Add test: outer frame wrapper element is present
  - [x] Add test: handle tab element is present
  - [x] Add test: two wheel guard elements are present
  - [x] Verify all existing tests continue to pass
- [x] Task 6: Run full test suite
  - [x] Run `npm run test:run` — all new and existing tests green

## Dev Notes

### Visualizer Exterior Architecture
[Source: docs/architecture/1-template-and-framework-selection.md#phase-25-framework-decisions]

Interior markup only — `div` elements; no SVG, no canvas, no new npm packages. `HousingUnitVisualizer` props interface is unchanged. The outer shell lives entirely inside the component's root element and is invisible to all consumers. Both interactive and read-only variants render the shell identically.

### Outer Shell Structure
[Source: docs/front-end-spec.md#screen-2]

```
┌──────────────────────────────┐
│        [handle tab]          │  ← centered tab, top edge
│  ┌────────────────────────┐  │
│  │  Slot 1  (empty/filled)│  │
│  ├────────────────────────┤  │
│  │  Slot 2                │  │
│  ├────────────────────────┤  │
│  │  ...                   │  │
│  │  Slot 6                │  │
│  └────────────────────────┘  │
│ [wheel]              [wheel] │  ← positioned outside frame
└──────────────────────────────┘
```

### Element Specifications
[Source: docs/front-end-spec.md#screen-2]

| Element | Spec |
|---------|------|
| Outer frame | `neutral-400` (`#9CA3AF`) bg; `rounded-xl` (12px); padding: `p-3` (12px all sides), `pb-2` (8px bottom) |
| Handle tab | 80px × 24px; hollow border (`border-neutral-400`, `bg-transparent`); 6px top corners, 0 bottom; centered horizontally; sibling div above frame with negative margin overlap |
| Wheel guards | 24px × 48px each; `neutral-600` (`#4B5563`) fill; `rounded-sm` (3px all corners, symmetric); absolute positioned outside frame (`-bottom-3 -left-6` / `-right-6`) |
| Slot grid | Unchanged — sits inside frame |

### Symmetric Border Radius
[Source: docs/architecture/1-template-and-framework-selection.md#phase-25-framework-decisions]

Wheel guards use `rounded-sm` (3px all corners, symmetric). No asymmetric border radius or inline styles needed.

### Handle Tab Positioning
The handle tab needs to visually protrude above the outer frame's top edge. Options:
- Negative top margin (`-mt-[14px]`) with `relative` positioning on the outer frame
- Absolute positioning within a `relative` outer frame wrapper
- CSS `transform: translateY(-100%)` on the handle tab

### Existing Container Card Compatibility
[Source: docs/front-end-spec.md#screen-1]
The outer shell sits inside the existing white card (`shadow-2`, `radius-lg`, `neutral-200` border, 16px padding) on `SubkitSelectionScreen`. The frame adds its own visual boundary within the card.

### No Epic 9 Dependency
[Source: docs/prd-phase2.5/5-epic-and-story-structure.md]
Story 10.1 has no Epic 9 dependency — can be worked in parallel after Story 9.1 merges. Only requires the base Phase 2 codebase.

### Files to Modify
- `src/components/visualizer/HousingUnitVisualizer.tsx` — Add outer frame, handle tab, wheel guards
- `tests/components/HousingUnitVisualizer.test.tsx` — Add 3 new tests for exterior elements

### Critical Constraints
[Source: docs/prd-phase2.5/2-requirements.md#compatibility-requirements]
- CR1: Props interface unchanged
- CR2: `SlotState` data model unchanged
- NFR7: Only `div` elements and CSS/Tailwind — no SVG, no canvas, no new dependencies

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test file: `tests/components/HousingUnitVisualizer.test.tsx`
- 3 new test cases as specified in AC8
- Use Vitest + React Testing Library
- Use `data-testid` or query by role/structure to locate new elements
- All existing HousingUnitVisualizer tests must pass without modification

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial story draft | Bob, SM Agent |
| 2026-03-10 | 1.1 | Implemented outer frame, handle tab, wheel guards with iterative visual refinements: centering/width fix, hollow handle, two-tone color scheme (neutral-400/600), flush wheel placement | James, Dev Agent |
| 2026-03-10 | 1.2 | Updated AC2, AC3, AC4, Tasks/Subtasks, and Dev Notes to match approved visual implementation per product owner sign-off on QA-identified spec deviations | Dev Agent |

## QA Results

### Review Date: 2026-03-10

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

The implementation is structurally sound. The component correctly adds the outer frame, handle tab, and wheel guards as child elements without modifying the props interface, `SlotState` model, or slot behavior. Code follows project conventions: named export, `FC<Props>` typing, no logic in JSX, `data-testid` attributes for testability. All 228 tests pass (15 HousingUnitVisualizer tests, including 3 new exterior element tests). Accessibility audits via axe show no violations.

However, the implementation deviates from the acceptance criteria specifications in several measurable dimensions. The change log documents these as "iterative visual refinements," suggesting they were intentional design adjustments. While the visual result is cohesive and polished, the specific AC values are not met.

### Spec Deviation Analysis

| Element | AC Spec | Actual Implementation | Severity |
|---------|---------|----------------------|----------|
| Outer frame background | `neutral-500` (#6B7280) | `neutral-400` (#9CA3AF) | Medium |
| Outer frame border radius | `radius-md` (10px) | `rounded-xl` (12px) | Low |
| Outer frame padding | 12px sides, 20px bottom, 28px top | `p-3` (12px all), `pb-2` (8px bottom) | Medium |
| Handle tab dimensions | 48px × 14px | `w-20` (80px) × `h-6` (24px) | Medium |
| Handle tab fill | Solid `neutral-500` fill | Hollow border (`border-neutral-400`, `bg-transparent`) | Medium |
| Handle tab border radius | 6px top, 0 bottom | `rounded-t-md` (6px top) | Low — matches |
| Wheel guard dimensions | 18px × 22px | `w-6` (24px) × `h-12` (48px) | Medium |
| Wheel guard positioning | Flush with base of outer frame | `-bottom-3 -left-6` / `-right-6` (outside frame) | Medium |
| Wheel guard corner radius | 6px outer corners, 0px inner | `rounded-sm` (3px all corners, symmetric) | Low |
| Handle tab structure | Child div of outer frame (AC3) | Sibling div rendered before outer frame | Medium |

### Refactoring Performed

None — deviations require UX/product decision before code changes.

### Compliance Check

- Coding Standards: ✓ Named export, FC typing, no logic in JSX, data-testid attributes, no snapshot tests
- Project Structure: ✓ Changes confined to `src/components/visualizer/HousingUnitVisualizer.tsx`; test file in correct location
- Testing Strategy: ✓ 3 new tests for exterior elements; axe accessibility assertions present; all 15 component tests pass
- All ACs Met: ✗ AC2 (frame color/padding), AC3 (handle dimensions/fill), AC4 (wheel dimensions/positioning/corners) deviate from spec values — see deviation table above. AC1, AC5, AC6, AC7, AC8 fully met.

### Improvements Checklist

- [ ] Reconcile outer frame background color with spec (neutral-500 vs current neutral-400) — requires UX decision
- [ ] Reconcile handle tab design (solid fill vs hollow border) — requires UX decision
- [ ] Align outer frame padding with spec (28px top, 20px bottom) or update AC if current values are preferred
- [ ] Align handle tab dimensions (48×14px) or update AC if current values are preferred
- [ ] Align wheel guard dimensions (18×22px) and positioning (flush, not outside frame) or update AC
- [ ] Apply asymmetric border radius on wheel guards (6px outer, 0px inner) per AC4
- [ ] Move handle tab inside outer frame div (AC3 specifies it as a child of the outer frame, currently rendered as a sibling)

### Security Review

No security concerns. Component is purely presentational with no user input handling, data processing, or network calls.

### Performance Considerations

No performance concerns. Implementation uses standard div elements with Tailwind classes. No new dependencies, no SVG/canvas, no expensive computations. GPU-composited properties only.

### Files Modified During Review

None.

### Gate Status

Gate: CONCERNS → docs/qa/gates/10.1-outer-frame-handle-tab-wheel-guards.yml

### Recommended Status

✗ Changes Required — Multiple acceptance criteria dimensions deviate from spec. The visual result is cohesive and may reflect intentional design improvements, but the ACs should either be updated to match the implementation or the implementation adjusted to match the ACs. Recommend UX/product review of the deviations before marking Done.

---

### Re-Review Date: 2026-03-10

### Reviewed By: Quinn (Test Architect)

### Re-Review Context

Product owner approved the current visual implementation. AC2, AC3, AC4, Tasks/Subtasks, and Dev Notes in this story file were updated (v1.2) to match the approved implementation values. The front-end spec (Screen 2 element table and Decisions Log entry #14) was also updated. This re-review validates the implementation against the updated acceptance criteria.

### Code Quality Assessment

Implementation remains structurally sound and unchanged since the initial review. The component correctly renders the outer frame, handle tab, and wheel guards using standard div elements with Tailwind CSS classes. Named export, `FC<Props>` typing, `data-testid` attributes for testability, and no logic in JSX. All 228 tests pass (15 HousingUnitVisualizer tests, including 3 new exterior element tests). Accessibility audits via axe show no violations.

### AC-to-Implementation Traceability

| AC | Requirement | Implementation | Status |
|----|-------------|----------------|--------|
| AC1 | Wrapper div inside component; props/SlotState unchanged; existing tests pass | `div[data-testid="outer-frame"]` wraps slot grid; props interface is `slots`, `readOnly`, `onSlotClick` — unchanged; all 12 pre-existing tests pass unmodified | ✓ PASS |
| AC2 | Outer frame: `neutral-400` (#9CA3AF), `rounded-xl` (12px), `p-3` (12px all), `pb-2` (8px bottom) | `bg-neutral-400 rounded-xl p-3 pb-2` | ✓ PASS |
| AC3 | Handle tab: sibling div above frame, 80×24px (`w-20`×`h-6`), hollow border (`border-neutral-400`, `bg-transparent`), 6px top/side borders, `rounded-t-md`, horizontally centered, negative margin overlap | `h-6 w-20 rounded-t-md border-t-[6px] border-x-[6px] border-neutral-400 bg-transparent mb-[-2px]`; centered via flex parent `items-center` | ✓ PASS |
| AC4 | Wheel guards: 24×48px (`w-6`×`h-12`), `neutral-600` (#4B5563), `rounded-sm` (3px symmetric), absolute positioned `-bottom-3 -left-6`/`-right-6` | Left: `absolute -bottom-3 -left-6 h-12 w-6 bg-neutral-600 rounded-sm`; Right: `absolute -bottom-3 -right-6 h-12 w-6 bg-neutral-600 rounded-sm` | ✓ PASS |
| AC5 | Both interactive and readOnly variants render shell identically | Shell elements render unconditionally regardless of `readOnly` prop | ✓ PASS |
| AC6 | Existing slot behaviors unaffected (cursor-pointer, hover:brightness-95, onSlotClick, empty non-interactive, readOnly non-interactive, animations) | Slot rendering delegated to `VisualizerSlot` — no changes to slot logic or styling | ✓ PASS |
| AC7 | Compatible with existing container card (white, shadow-2, radius-lg, neutral-200 border, 16px padding) | Shell sits inside component root element; no changes to how the component is consumed by `SubkitSelectionScreen` or `SummaryScreen` | ✓ PASS |
| AC8 | Tests: outer frame present, handle tab present, two wheel guards present; existing tests pass | 3 new tests query `data-testid` attributes; all 15 component tests pass | ✓ PASS |

### Previous Spec Deviations — Resolved

All deviations identified in the initial review have been resolved by updating the acceptance criteria and front-end spec to match the product-owner-approved implementation:

| Element | Original AC Spec | Updated AC Spec (v1.2) | Implementation | Status |
|---------|-----------------|------------------------|----------------|--------|
| Outer frame background | `neutral-500` | `neutral-400` (#9CA3AF) | `bg-neutral-400` | ✓ Resolved |
| Outer frame border radius | `radius-md` (10px) | `rounded-xl` (12px) | `rounded-xl` | ✓ Resolved |
| Outer frame padding | 28px top, 12px sides, 20px bottom | `p-3`/`pb-2` | `p-3 pb-2` | ✓ Resolved |
| Handle tab dimensions | 48×14px | 80×24px (`w-20`×`h-6`) | `w-20 h-6` | ✓ Resolved |
| Handle tab fill | Solid `neutral-500` | Hollow border (`border-neutral-400`, `bg-transparent`) | `border-neutral-400 bg-transparent` | ✓ Resolved |
| Wheel guard dimensions | 18×22px | 24×48px (`w-6`×`h-12`) | `w-6 h-12` | ✓ Resolved |
| Wheel guard positioning | Flush with base | Absolute, negative offsets | `-bottom-3 -left-6`/`-right-6` | ✓ Resolved |
| Wheel guard corners | 6px outer, 0px inner (asymmetric) | `rounded-sm` (3px symmetric) | `rounded-sm` | ✓ Resolved |
| Handle tab structure | Child of outer frame | Sibling div above frame | Sibling div with `mb-[-2px]` overlap | ✓ Resolved |

### Refactoring Performed

None required — implementation fully meets all updated acceptance criteria.

### Compliance Check

- Coding Standards: ✓ Named export, FC typing, no logic in JSX, data-testid attributes, no snapshot tests
- Project Structure: ✓ Changes confined to `src/components/visualizer/HousingUnitVisualizer.tsx`; test file in correct location
- Testing Strategy: ✓ 3 new tests for exterior elements; axe accessibility assertions present; all 15 component tests pass
- All ACs Met: ✓ AC1–AC8 fully met per updated v1.2 acceptance criteria

### Improvements Checklist

- [x] All previous improvement items resolved via AC/spec update (product owner approved)

### Security Review

No security concerns. Component is purely presentational with no user input handling, data processing, or network calls.

### Performance Considerations

No performance concerns. Implementation uses standard div elements with Tailwind classes. No new dependencies, no SVG/canvas, no expensive computations.

### Files Modified During Review

None.

### Gate Status

Gate: PASS → docs/qa/gates/10.1-outer-frame-handle-tab-wheel-guards.yml

### Recommended Status

✓ Ready for Done — All 8 acceptance criteria are fully met against the updated v1.2 specifications. Implementation, front-end spec, and story ACs are aligned. No outstanding issues.

# Story 5.1: Kit Summary Display

## Status: Done

## Story

**As a** user,
**I want** to see a complete, organized overview of my configured kit,
**so that** I can review everything I have selected and feel confident my kit is complete.

## Acceptance Criteria

1. The Summary Page displays all selected subkits in the order they were configured
2. Each subkit section shows the name, assigned color accent, size, and all included items with quantities
3. Subkits configured as empty containers are clearly labeled as Empty Container
4. Custom subkit items are displayed grouped under the Custom category label
5. Overall kit configuration shown at a glance — total subkits selected, total slots used out of 6
6. The Summary Page is clean, well-spaced, and visually organized

## Tasks / Subtasks

- [ ] Task 1: Implement SummaryScreen layout (AC: 1, 5, 6)
  - [ ] Update `src/components/summary/SummaryScreen.tsx`
  - [ ] Heading: "Review Your Kit"
  - [ ] Focus heading on mount
  - [ ] Display total subkits selected and total slots used ("N subkits using M of 6 slots")
  - [ ] Import `print.css` for print-ready layout
- [ ] Task 2: Create SubkitSummarySection component (AC: 2, 3, 4)
  - [ ] Create `src/components/summary/SubkitSummarySection.tsx`
  - [ ] Per-subkit card with category color bar on left/top
  - [ ] Display: subkit name, size (Regular/Large), category icon
  - [ ] List all included items with quantities
  - [ ] Empty container subkits: display "Empty Container" badge instead of item list
  - [ ] Custom subkit: items grouped under Custom category label
- [ ] Task 3: Render all subkits in config order (AC: 1)
  - [ ] Map over `selectedSubkits` ordered by `selectionOrder`
  - [ ] For each: render SubkitSummarySection with items from store
  - [ ] Items from `itemSelections` filtered by subkitId
- [ ] Task 4: Handle empty containers in display (AC: 3)
  - [ ] Check `emptyContainers` array from store
  - [ ] If subkit is empty container: show badge/indicator, no item list
  - [ ] Badge uses category color with neutral styling
- [ ] Task 5: Style for readability (AC: 6)
  - [ ] Clean spacing between sections
  - [ ] Summary cards use `var(--shadow-1)` elevation
  - [ ] Color bars/accents use category colorBase
  - [ ] Well-structured hierarchy: section → subkit name → items

## Dev Notes

### Brownfield Correction: SummaryPage.tsx
[Source: docs/architecture/1-template-and-framework-selection.md]
- `SummaryPage.tsx` is corrected in Epic 5 — replaced by `SummaryScreen.tsx`
- Key corrections: add HousingUnitVisualizer readOnly (Story 5.2), add "Get My Kit" CTA (Story 5.3), replace .txt export with window.print() (Story 5.4), remove weight/volume display

### Store Data Access
[Source: docs/architecture/5-state-management.md#store]
- `selectedSubkits`: ordered by `selectionOrder`
- `itemSelections`: keyed as `${subkitId}::${itemId}`, filter by subkitId
- `emptyContainers`: array of subkitIds

### Print CSS
[Source: docs/architecture/8-styling-guidelines.md#print-css]
- `print.css` imported ONLY in SummaryScreen — not global
- Hides: header, step indicator, CTA buttons, edit/print/start-over buttons
- `page-break-inside: avoid` on `.summary-subkit-section`

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test subkit sections render in order
- Test items display with quantities
- Test empty container badge
- axe assertion required

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |

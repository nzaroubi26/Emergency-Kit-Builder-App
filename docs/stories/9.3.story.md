# Story 9.3: SubkitStatsStrip Component

## Status: Draft

## Story

**As a** user,
**I want** to see estimated weight and volume fill for my current subkit as I select items,
**so that** I have a real-time sense of how much I am packing and how full my container is getting.

## Acceptance Criteria

1. A new `SubkitStatsStrip` component is created at `src/components/item-config/SubkitStatsStrip.tsx` with the following props interface:
   - `weightLbs: number` — pre-computed by parent via `calculateSubkitWeightLbs`
   - `volumePct: number` — pre-computed by parent via `calculateSubkitVolumePct` (unclamped integer)
   - `categoryColor: string` — category base hex; used for volume bar fill via inline style
   The component has no internal state and no store dependency.

2. The strip renders as a full-width row with:
   - Background: `neutral-100` (`#F3F4F6`)
   - Border radius: `radius-md` (10px)
   - Padding: `py-2 px-3`
   - Bottom margin: `mb-3` (gap before item grid)
   - Layout: `flex items-center justify-between gap-4`

3. Left side — weight label: `~{weightLbs.toFixed(1)} lbs` in `text-caption` (12px/400), `neutral-500` color.

4. Right side — volume group (`flex items-center gap-2`):
   - Bar track: `width: 120px`, `height: 6px`, `neutral-200` background, `radius-full`.
   - Bar fill: category base color via inline `style={{ backgroundColor: categoryColor, width: Math.min(volumePct, 100) + '%' }}`, `radius-full`, `transition: width 150ms cubic-bezier(0.4,0,0.2,1)`.
   - Label: `{volumePct}% filled` in `text-caption`, `neutral-500`. Label shows true unclamped value; bar width is clamped to 100%.

5. Accessibility attributes:
   - Wrapper `div`: `aria-label` = `"Subkit stats — {weightLbs.toFixed(1)} lbs, {volumePct}% of container capacity filled"`
   - Volume bar track: `role="progressbar"`, `aria-valuenow={volumePct}`, `aria-valuemin={0}`, `aria-valuemax={100}`, `aria-label="Container volume"`

6. `ItemConfigScreen` renders `<SubkitStatsStrip>` between the subkit heading/accent bar and the item grid. It derives `weightLbs` and `volumePct` inline from existing `itemSelections` store data and the current subkit's size. `containerCapacityIn3` is 1,728 for Regular and 3,456 for Large.

7. `CustomSubkitScreen` receives identical `SubkitStatsStrip` treatment. Container capacity is derived from the custom subkit's selected size.

8. In empty container state the parent passes `weightLbs={0}` and `volumePct={0}` — the strip displays `~0.0 lbs` and `0% filled` with an empty bar.

9. Both values update live: because `weightLbs` and `volumePct` are derived inline from `itemSelections` (already subscribed to by the parent), toggling any item or changing any quantity causes an immediate re-render with updated values.

10. A component test `tests/components/SubkitStatsStrip.test.tsx` covers:
    - Correct weight label rendered for `weightLbs={2.3}` → `~2.3 lbs`
    - Correct volume label rendered for `volumePct={48}` → `48% filled`
    - Bar fill width clamped at 100% for `volumePct={112}` while label still shows `112% filled`
    - Correct `aria-label` on wrapper for given weight + pct inputs
    - Correct `aria-valuenow` on bar track
    - `axe-core` accessibility assertion passes

## Tasks / Subtasks

- [ ] Task 1: Create SubkitStatsStrip component (AC: 1, 2, 3, 4, 5)
  - [ ] Create `src/components/item-config/SubkitStatsStrip.tsx`
  - [ ] Define `SubkitStatsStripProps` interface: `weightLbs`, `volumePct`, `categoryColor`
  - [ ] Named export `SubkitStatsStrip: FC<SubkitStatsStripProps>`
  - [ ] No internal state, no store dependency
  - [ ] Render strip container: `neutral-100` bg, `rounded-[10px]`, `py-2 px-3`, `mb-3`, `flex items-center justify-between gap-4`
  - [ ] Left side: weight label `~{weightLbs.toFixed(1)} lbs`, `text-caption`, `neutral-500`
  - [ ] Right side: volume group `flex items-center gap-2`
  - [ ] Bar track: 120px wide, 6px tall, `neutral-200` bg, `rounded-full`
  - [ ] Bar fill: inline `style={{ backgroundColor: categoryColor, width: Math.min(volumePct, 100) + '%' }}`, `rounded-full`, `transition: width 150ms cubic-bezier(0.4,0,0.2,1)`
  - [ ] Volume label: `{volumePct}% filled`, `text-caption`, `neutral-500`
  - [ ] Add `aria-label` on wrapper div
  - [ ] Add `role="progressbar"`, `aria-valuenow`, `aria-valuemin={0}`, `aria-valuemax={100}`, `aria-label="Container volume"` on bar track
- [ ] Task 2: Integrate into ItemConfigScreen (AC: 6, 8, 9)
  - [ ] Import `SubkitStatsStrip` and `calculateSubkitWeightLbs`, `calculateSubkitVolumePct` from `slotCalculations`
  - [ ] Define constants: `REGULAR_CAPACITY_IN3 = 1728`, `LARGE_CAPACITY_IN3 = 3456`
  - [ ] Derive `weightLbs` and `volumePct` inline from `itemSelections` store data and current subkit's size
  - [ ] In empty container state, pass `weightLbs={0}` and `volumePct={0}`
  - [ ] Render `<SubkitStatsStrip>` between subkit heading/accent bar and item grid
  - [ ] Pass `categoryColor` from the current subkit's category
- [ ] Task 3: Integrate into CustomSubkitScreen (AC: 7, 8, 9)
  - [ ] Same imports and constants as ItemConfigScreen
  - [ ] Derive values using all items from all categories in the Custom subkit
  - [ ] Container capacity from the custom subkit's selected size
  - [ ] Render `<SubkitStatsStrip>` between heading and item grid
  - [ ] Pass custom subkit's category color
- [ ] Task 4: Write 6 component tests (AC: 10)
  - [ ] Create `tests/components/SubkitStatsStrip.test.tsx`
  - [ ] Test: weight label `~2.3 lbs` for `weightLbs={2.3}`
  - [ ] Test: volume label `48% filled` for `volumePct={48}`
  - [ ] Test: bar fill clamped at 100% for `volumePct={112}`, label shows `112% filled`
  - [ ] Test: correct `aria-label` on wrapper
  - [ ] Test: correct `aria-valuenow` on bar track
  - [ ] Test: `axe-core` accessibility passes
- [ ] Task 5: Verify all existing tests pass
  - [ ] Run `npm run test:run` — all new and existing tests green
  - [ ] Verify existing `ItemConfigScreen` and `CustomSubkitScreen` tests pass without modification

## Dev Notes

### SubkitStatsStrip Component Architecture
[Source: docs/architecture/6-data-layer.md#subkitstatsstrip-component-spec]

The architecture defines the following props interface — this supersedes the UX spec's Component Library entry which lists raw `weightGrams`/`volumeIn3` props:

```typescript
interface SubkitStatsStripProps {
  weightLbs: number;
  volumePct: number;
  categoryColor: string;
}
```

The component has zero internal state. All values are pre-computed by the parent using `calculateSubkitWeightLbs` and `calculateSubkitVolumePct`.

### Rendering Rules
[Source: docs/architecture/6-data-layer.md#subkitstatsstrip-component-spec]
- Weight label: `` `~${weightLbs.toFixed(1)} lbs` `` — `text-caption`, `neutral-500`
- Bar fill: `style={{ backgroundColor: categoryColor, width: Math.min(volumePct, 100) + '%' }}` — `rounded-full`, transition 150ms
- Volume label: `` `${volumePct}% filled` `` — true unclamped integer
- Bar width is clamped; label is not — the two may diverge above 100%
- Dynamic `backgroundColor` always via inline `style` prop — never Tailwind arbitrary values (Rule 8)

### Critical Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md#phase-25-coding-standards]
- **Rule 8:** Volume bar fill color uses `style={{ backgroundColor: categoryColor }}`. Never use Tailwind arbitrary values for category colors.
- **Rule 13:** `weightLbs` and `volumePct` are always computed inline in the parent. Never stored in Zustand and never lifted into component state.
- **FR13:** No warnings, no thresholds, no color changes at any weight or volume value. Purely informational at all values including >100%.

### Empty Container State
[Source: docs/architecture/6-data-layer.md#subkitstatsstrip-component-spec]
Parent passes `weightLbs={0}` and `volumePct={0}`. Strip renders `~0.0 lbs` and `0% filled` with empty bar. No special branch in the component.

### Import Patterns
[Source: docs/architecture/11-frontend-developer-standards.md#phase-25-import-patterns]
```typescript
import { calculateSubkitWeightLbs, calculateSubkitVolumePct } from '../utils/slotCalculations';
import { SubkitStatsStrip } from './SubkitStatsStrip';
const REGULAR_CAPACITY_IN3 = 1728;
const LARGE_CAPACITY_IN3 = 3456;
```

### Dependency: Stories 9.1 and 9.2
This story requires both Story 9.1 (`weightGrams`/`volumeIn3` on `KitItem`) and Story 9.2 (`calculateSubkitWeightLbs`/`calculateSubkitVolumePct` functions) to be complete.

### Files to Create
- `src/components/item-config/SubkitStatsStrip.tsx`
- `tests/components/SubkitStatsStrip.test.tsx`

### Files to Modify
- `src/components/item-config/ItemConfigScreen.tsx` — Import and render SubkitStatsStrip
- `src/components/item-config/CustomSubkitScreen.tsx` — Import and render SubkitStatsStrip

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test file: `tests/components/SubkitStatsStrip.test.tsx`
- 6 test cases as specified in AC10
- Use Vitest + React Testing Library + axe-core
- Test rendering, accessibility attributes, bar clamping behavior
- All existing `ItemConfigScreen` and `CustomSubkitScreen` tests must pass without modification

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial story draft | Bob, SM Agent |

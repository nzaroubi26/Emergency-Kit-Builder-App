# Story 9.4: Summary Page Weight and Volume Readout

## Status: Done

## Story

**As a** user,
**I want** to see total kit weight and per-subkit weight and volume on the Summary Page,
**so that** I have a complete physical overview of my configured kit before purchasing.

## Acceptance Criteria

1. A kit-level stats row is added at the top of the Kit Summary section on `SummaryScreen`, directly above the list of `SubkitSummarySection` components:
   - Background: `neutral-100` pill, `radius-md`, `py-2 px-4`, `mb-4` margin below
   - Layout: `flex items-center gap-3`, `text-caption`, `neutral-500`
   - Total weight: `~{totalKitWeightLbs} lbs total` — one decimal, tilde prefix, "total" suffix; leftmost item
   - Separator: `·` (middle dot) in `neutral-300`, `mx-1`
   - Subkit count: `{nonEmptySubkitCount} subkits configured` — integer count of subkits not in empty container state

2. Total kit weight is computed as the sum of `calculateSubkitWeightLbs()` across all selected subkits. This is inline derived state — no new store fields.

3. Each `SubkitSummarySection` heading row is updated to display per-subkit stats right-aligned via `ml-auto`, appended after the existing category name + size badge:
   - `~{subkitWeightLbs} lbs · {subkitVolumePct}%` in `text-caption`, `neutral-400`
   - A mini volume bar: 40px wide × 4px tall `neutral-200` track with category base color fill (inline style), `radius-full`, `ml-1`, bar width clamped at 100%

4. Subkits in empty container state display `~0.0 lbs · 0%` with an empty mini bar — consistent with non-empty subkits.

5. Weight and volume stats are included in the `@media print` output. No additional print CSS is needed — they are part of the normal DOM and inherit existing print rules.

6. Star ratings are not introduced or affected by this story. `SubkitSummarySection` continues to not render `StarRating` per Phase 2 architecture rule 14.

7. `SummaryScreen` component test is extended with:
   - Kit-level stats row present with correct total weight and subkit count
   - Per-subkit inline stats (`~X.X lbs · XX%`) present in each `SubkitSummarySection` heading
   - Stats row visible in a mocked print snapshot (or verified not hidden by print CSS)

## Tasks / Subtasks

- [ ] Task 1: Add kit-level stats row to SummaryScreen (AC: 1, 2)
  - [ ] Import `calculateSubkitWeightLbs` and `calculateSubkitVolumePct` from `slotCalculations`
  - [ ] Import `ITEMS` (or relevant item arrays) from `kitItems.ts`
  - [ ] Define `REGULAR_CAPACITY_IN3 = 1728` and `LARGE_CAPACITY_IN3 = 3456` constants
  - [ ] Derive `totalKitWeightLbs` by summing `calculateSubkitWeightLbs()` across all selected subkits — inline derived state, no new store fields
  - [ ] Derive `nonEmptySubkitCount` — count of subkits not in empty container state
  - [ ] Render stats row pill: `neutral-100` bg, `rounded-[10px]`, `py-2 px-4`, `mb-4`
  - [ ] Layout: `flex items-center gap-3`, `text-caption`, `neutral-500`
  - [ ] Total weight: `~{totalKitWeightLbs.toFixed(1)} lbs total`
  - [ ] Separator: `·` in `neutral-300`, `mx-1`
  - [ ] Subkit count: `{nonEmptySubkitCount} subkits configured`
  - [ ] Position directly above `SubkitSummarySection` list
- [ ] Task 2: Add per-subkit inline stats to SubkitSummarySection (AC: 3, 4)
  - [ ] Compute `subkitWeightLbs` and `subkitVolumePct` for each subkit in SummaryScreen
  - [ ] Pass weight and volume values as new props to `SubkitSummarySection`
  - [ ] In `SubkitSummarySection` heading row, append after category name + size badge:
  - [ ] `ml-auto` wrapper: `~{subkitWeightLbs} lbs · {subkitVolumePct}%` in `text-caption`, `neutral-400`
  - [ ] Mini volume bar: 40px wide × 4px tall, `neutral-200` track, category color fill, `rounded-full`, `ml-1`
  - [ ] Bar width clamped: `Math.min(subkitVolumePct, 100) + '%'`
  - [ ] Category color via inline style (Rule 8)
  - [ ] Empty container subkits: `~0.0 lbs · 0%` with empty mini bar
- [ ] Task 3: Verify print compatibility (AC: 5)
  - [ ] Confirm stats are in the normal DOM and not hidden by print CSS
  - [ ] No additional print CSS changes needed
- [ ] Task 4: Verify no StarRating in SubkitSummarySection (AC: 6)
  - [ ] Confirm `SubkitSummarySection` does not import or render `StarRating`
  - [ ] Rule 14 compliance verified
- [ ] Task 5: Extend SummaryScreen tests (AC: 7)
  - [ ] Add test: kit-level stats row present with correct total weight and subkit count
  - [ ] Add test: per-subkit inline stats present in each `SubkitSummarySection` heading
  - [ ] Add test: stats row not hidden by print CSS (or visible in mocked print snapshot)
- [ ] Task 6: Verify all existing tests pass
  - [ ] Run `npm run test:run` — all new and existing tests green
  - [ ] Verify existing `SummaryScreen` tests pass without modification
  - [ ] Verify `Get My Kit` CTA, `Edit My Kit`, `Print`, and `Start Over` flows unaffected

## Dev Notes

### Summary Page Stats Layout
[Source: docs/prd-phase2.5/6-epic-details.md#story-94]

Kit-level stats row sits above the subkit list as a `neutral-100` pill. Per-subkit stats sit inline in each `SubkitSummarySection` heading row using `ml-auto`.

### Total Weight Computation
[Source: docs/architecture/6-data-layer.md#new-pure-functions]
- Sum of `calculateSubkitWeightLbs()` across all selected subkits
- Each subkit call passes the items for that subkit's category, the store `itemSelections`, and the subkitId
- This is inline derived state — no new store fields per Rule 13

### Per-Subkit Stats
[Source: docs/prd-phase2.5/6-epic-details.md#story-94]
- Weight: `calculateSubkitWeightLbs(items, itemSelections, subkitId)` → `~X.X lbs`
- Volume: `calculateSubkitVolumePct(items, itemSelections, subkitId, capacityIn3)` → `XX%`
- Mini bar: 40px × 4px, `neutral-200` track, category base color fill, clamped at 100% width
- Category color applied via inline `style` (Rule 8)

### Critical Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md#phase-25-coding-standards]
- **Rule 8:** Dynamic category colors via inline `style` — never Tailwind arbitrary values
- **Rule 13:** Derived state not stored — compute inline from `itemSelections`
- **Rule 14:** No `StarRating` in `SubkitSummarySection` — unchanged in Phase 2.5
- **FR13:** No warnings at any weight or volume value. Purely informational.

### SubkitSummarySection Props Update
[Source: docs/architecture/3-project-structure.md]
- `SubkitSummarySection` will need new props for weight, volume, and category color
- Keep existing props interface backward-compatible — new props can be optional or always provided by the parent

### Import Patterns
[Source: docs/architecture/11-frontend-developer-standards.md#phase-25-import-patterns]
```typescript
import { calculateSubkitWeightLbs, calculateSubkitVolumePct } from '../utils/slotCalculations';
const REGULAR_CAPACITY_IN3 = 1728;
const LARGE_CAPACITY_IN3 = 3456;
```

### Dependency: Stories 9.1, 9.2, 9.3
This story requires Stories 9.1 (data fields), 9.2 (calculation functions), and 9.3 (SubkitStatsStrip proven pattern) to be complete.

### Files to Modify
- `src/components/summary/SummaryScreen.tsx` — Add kit-level stats row, compute per-subkit stats
- `src/components/summary/SubkitSummarySection.tsx` — Add per-subkit inline weight/volume stats to heading row

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Extend existing `SummaryScreen` component tests
- 3 new test cases as specified in AC7
- Use Vitest + React Testing Library
- All existing `SummaryScreen` tests must pass without modification
- Verify `Get My Kit`, `Edit My Kit`, `Print`, `Start Over` flows unaffected

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial story draft | Bob, SM Agent |

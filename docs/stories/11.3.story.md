# Story 11.3: CartSidebar Live Content

## Status: Draft

## Story

**As a** user,
**I want** the cart sidebar to display my selected subkits and items with prices, quantities I can adjust, and items I can remove,
**so that** I always know exactly what is in my kit and how much it will cost.

## Acceptance Criteria

1. **AC1 — CartSidebar body is replaced with live subkit sections.**
   The Story 11.2 placeholder `<p>Your cart is empty.</p>` is removed. The body renders the following:

   *When `selectedSubkits` is empty or all subkits are empty-container:*
   An empty-state message: `"Your kit is empty. Start by selecting subkits."` in `text-body` (14px / 400), `neutral-400`, centered vertically and horizontally within the body area.

   *When at least one subkit exists:*
   One section per subkit in `selectedSubkits`, ordered by `selectionOrder` ascending. Each section is described in AC2–AC6.

2. **AC2 — Data derivation inside CartSidebar.**
   CartSidebar reads the following from `useKitStore()` — no other store fields accessed:
   ```typescript
   const { selectedSubkits, itemSelections, emptyContainers } = useKitStore();
   const { toggleItem, setItemQuantity } = useKitStore();
   ```
   CartSidebar also imports `ITEMS` from `src/data/kitItems.ts` directly (not from the store) to look up `pricePlaceholder` and `name` by item ID. CartSidebar imports `calculateSubkitCartTotal` and `calculateCartGrandTotal` from `src/utils/cartCalculations.ts`.

3. **AC3 — Subkit section header row.**
   Each section renders a header row containing:
   - Left: category icon (resolved via `getCategoryIcon` + `resolveIcon`, size 16) + subkit name in `text-label` (14px / 500), `neutral-900`
   - Right: container price badge — `"$40"` (Regular) or `"$60"` (Large) in `text-caption` (12px / 400), `neutral-500`, preceded by size label `"Regular"` / `"Large"` in the same style
   - The header row uses the subkit's category base color as a `4px` left border. Dynamic color via inline `style={{ borderLeftColor: categoryBaseColor }}` — never a Tailwind arbitrary value.
   - Bottom border: `border-b border-[var(--color-neutral-100)]`, `mb-1`

4. **AC4 — Empty-container subkit body.**
   If `emptyContainers.includes(subkit.subkitId)`, the section body renders a single row below the header:
   `"◈ Empty container"` in `text-caption`, `neutral-400`, `italic`, `py-2 px-1`. No item rows. Section total = container price only.

5. **AC5 — Item rows for included items.**
   For every entry in `itemSelections` where `sel.subkitId === subkit.subkitId` and `sel.included === true`, one item row is rendered. Rows are sorted by `sel.itemId` alphabetically. Each item row contains:

   - *Item name:* `text-body` (14px / 400), `neutral-700`. Truncated to one line via `truncate` if too long.
   - *Quantity controls* (`flex items-center gap-1`): `−` button | quantity display | `+` button. Min 1, max 10. `−` disabled when `quantity === 1`. `+` disabled when `quantity === 10`.
     - `−` calls `setItemQuantity(sel.subkitId, sel.itemId, sel.quantity - 1)`.
     - `+` calls `setItemQuantity(sel.subkitId, sel.itemId, sel.quantity + 1)`.
     - Button size: `w-6 h-6`, `rounded-sm`, `border border-[var(--color-neutral-200)]`, `text-caption`, `neutral-600`. Disabled state: `opacity-40 cursor-not-allowed`.
   - *Line total:* `text-caption`, `neutral-500`, right-aligned. Formatted as `$XX.XX` using `(price * quantity).toFixed(2)`. If `pricePlaceholder` is null, shows `"—"`.
   - *Remove button:* `X` icon from lucide-react (size 12), `aria-label="Remove {itemName} from cart"`. Calls `toggleItem(sel.subkitId, sel.itemId)` which removes the item's selection entry from `itemSelections` entirely (deletes the key). Styled as a ghost icon button: `text-[var(--color-neutral-400)] hover:text-[var(--color-neutral-700)]`, no border, no background.

6. **AC6 — Subkit section subtotal row.**
   Below the item rows (or below the empty-container row), a subtotal row renders:
   - `flex justify-between items-center`, `pt-2 mt-1 border-t border-[var(--color-neutral-100)]`
   - Left: `"Subtotal"` in `text-caption`, `neutral-500`
   - Right: `"$XX.XX"` using `calculateSubkitCartTotal(subkit, itemSelections, ITEMS).toFixed(2)`, in `text-label` (14px / 500), `neutral-700`

7. **AC7 — Separator between subkit sections.**
   Each section (except the last) is separated by a `<hr>` with `border-neutral-100` or a `my-3` spacer div.

8. **AC8 — CartSidebar footer replaced with live grand total.**
   The Story 11.2 placeholder footer is replaced:
   - Left: `"Total"` in `text-label`, `neutral-700`
   - Right: grand total formatted as `$XX.XX` using `calculateCartGrandTotal(selectedSubkits, itemSelections, ITEMS).toFixed(2)`, in `text-h3` (18px / 600), `neutral-900`
   - Below the total row: `text-caption`, `neutral-400`, `mt-1`: `"Container prices included"`

9. **AC9 — Cart item row category color applied via inline style.**
   Each item row has a subtle `4px` left border accent in the subkit's category base color applied as `style={{ borderLeftColor: categoryBaseColor }}` on the row wrapper div. Never use Tailwind arbitrary values for this color (Rule 8).

10. **AC10 — Live updates without lag.**
    Because CartSidebar reads directly from `useKitStore()`, any item toggle or quantity change on `ItemConfigScreen` causes CartSidebar to re-render immediately. No polling or event bus required — Zustand's subscription model provides this automatically.

11. **AC11 — Component tests in `tests/components/CartSidebar.test.tsx` extended.**
    All Story 11.2 tests continue to pass. The following additional tests are added:

    | Test | Assertion |
    |---|---|
    | Empty state: no subkits → renders empty message | `screen.getByText(/your kit is empty/i)` present |
    | One subkit, one included item → item name rendered | item name text present in DOM |
    | One subkit, one included item → line total formatted correctly | `$XX.XX` string rendered for known price × quantity |
    | Subkit subtotal row reflects container price + item costs | subtotal element contains correct `$XX.XX` |
    | Grand total equals sum of all subkit subtotals | footer total element correct |
    | Clicking `−` button calls `setItemQuantity` with `quantity - 1` | mock store action called with correct args |
    | Clicking remove `X` icon calls `toggleItem` | mock store action called with correct subkitId + itemId |
    | Empty-container subkit shows "◈ Empty container" and no item rows | correct text present; no quantity controls in DOM |
    | axe-core assertion passes with populated cart content | zero violations |

## Tasks / Subtasks

- [ ] Task 1: Replace CartSidebar body with live subkit sections (AC: 1, 2, 3, 4, 5, 6, 7, 9)
  - [ ] Add new imports to `CartSidebar.tsx`: `useKitStore` from `../../store/kitStore`, `ITEMS` from `../../data/kitItems`, `CONTAINER_PRICES`, `calculateSubkitCartTotal`, `calculateCartGrandTotal` from `../../utils/cartCalculations`, `getCategoryColor`, `getCategoryIcon` from `../../utils/categoryUtils`, `resolveIcon` from `../../utils/iconResolver`
  - [ ] Read store state inside component: destructure `selectedSubkits`, `itemSelections`, `emptyContainers`, `toggleItem`, `setItemQuantity` from `useKitStore()`
  - [ ] Sort subkits by `selectionOrder` ascending before rendering
  - [ ] Replace the placeholder body `<p>Your cart is empty.</p>` with conditional rendering:
    - If no subkits or all subkits are in `emptyContainers` with no included items: render empty-state message `"Your kit is empty. Start by selecting subkits."` centered, `text-[14px]`, `text-[var(--color-neutral-400)]`
    - Otherwise: render one section per subkit
  - [ ] For each subkit section, render the header row:
    - Look up category icon via `getCategoryIcon(subkit.categoryId)` → `resolveIcon(iconName)` to get the LucideIcon component, render at size 16
    - Look up category name via `CATEGORIES[subkit.categoryId]?.name` (using the already-imported `CATEGORIES` from `../../data/kitItems`)
    - Look up category base color via `getCategoryColor(subkit.categoryId)?.base`
    - Apply `4px` left border with inline `style={{ borderLeftColor: categoryBaseColor }}`
    - Show container size label (`"Regular"` / `"Large"`) and price (`$${CONTAINER_PRICES[subkit.size]}`) on the right
    - Apply `border-b border-[var(--color-neutral-100)] mb-1`
  - [ ] For empty-container subkits (`emptyContainers.includes(subkit.subkitId)`): render `"◈ Empty container"` row in `text-[12px]`, `text-[var(--color-neutral-400)]`, `italic`, `py-2 px-1`
  - [ ] For non-empty subkits, filter and sort included items:
    ```typescript
    const includedItems = Object.values(itemSelections)
      .filter(sel => sel.subkitId === subkit.subkitId && sel.included)
      .sort((a, b) => a.itemId.localeCompare(b.itemId));
    ```
  - [ ] For each included item row:
    - Look up item details: `const item = ITEMS.find(i => i.id === sel.itemId)`
    - Render item name with `truncate` class, `text-[14px]`, `text-[var(--color-neutral-700)]`
    - Render quantity controls: `−` button (disabled when `quantity === 1`), quantity display, `+` button (disabled when `quantity === 10`)
    - `−` calls `setItemQuantity(sel.subkitId, sel.itemId, sel.quantity - 1)`, `+` calls `setItemQuantity(sel.subkitId, sel.itemId, sel.quantity + 1)`
    - Button styling: `w-6 h-6 rounded-sm border border-[var(--color-neutral-200)] text-[12px] text-[var(--color-neutral-600)]`, disabled: `opacity-40 cursor-not-allowed`
    - Render line total: `$${(item.pricePlaceholder * sel.quantity).toFixed(2)}` or `"—"` if `pricePlaceholder` is null. Style: `text-[12px]`, `text-[var(--color-neutral-500)]`
    - Render remove button: `X` icon (size 12), `aria-label="Remove {item.name} from cart"`, calls `toggleItem(sel.subkitId, sel.itemId)`
    - Apply `4px` left border accent via `style={{ borderLeftColor: categoryBaseColor }}` on the row wrapper (AC9)
  - [ ] Render subtotal row below items: `"Subtotal"` left, `$${calculateSubkitCartTotal(subkit, itemSelections, ITEMS).toFixed(2)}` right
  - [ ] Add `<hr className="border-[var(--color-neutral-100)] my-3" />` separator between sections (not after the last one)

- [ ] Task 2: Replace CartSidebar footer with live grand total (AC: 8)
  - [ ] Replace the static `"$0.00"` footer with `calculateCartGrandTotal(selectedSubkits, itemSelections, ITEMS).toFixed(2)`
  - [ ] Update "Total" label style: `text-[14px] font-medium text-[var(--color-neutral-700)]`
  - [ ] Update grand total value style: `text-[18px] font-semibold text-[var(--color-neutral-900)]`
  - [ ] Add `"Container prices included"` note below: `text-[12px]`, `text-[var(--color-neutral-400)]`, `mt-1`

- [ ] Task 3: Extend `tests/components/CartSidebar.test.tsx` (AC: 11)
  - [ ] Add `vi.mock('../../src/store/kitStore')` to mock `useKitStore`
  - [ ] Create a helper function to set up mock store return values with `selectedSubkits`, `itemSelections`, `emptyContainers`, `toggleItem: vi.fn()`, `setItemQuantity: vi.fn()`
  - [ ] Ensure all 6 existing Story 11.2 tests still pass (they need the mock to return empty state)
  - [ ] Add test: Empty state — no subkits → renders `"Your kit is empty. Start by selecting subkits."`
  - [ ] Add test: One subkit with one included item → item name rendered in DOM
  - [ ] Add test: One subkit with one included item → line total formatted as `$XX.XX` for known price × quantity
  - [ ] Add test: Subkit subtotal row reflects container price + item costs
  - [ ] Add test: Grand total equals sum of all subkit subtotals
  - [ ] Add test: Clicking `−` button calls `setItemQuantity` with `(subkitId, itemId, quantity - 1)`
  - [ ] Add test: Clicking remove `X` icon calls `toggleItem` with `(subkitId, itemId)`
  - [ ] Add test: Empty-container subkit shows `"◈ Empty container"` and no quantity controls in DOM
  - [ ] Add test: axe-core assertion passes with populated cart content (zero violations)

- [ ] Task 4: Run full verification suite (AC: all)
  - [ ] Run `npm run typecheck` — zero errors
  - [ ] Run `npm run test:run` — all new and existing tests green
  - [ ] Run `npm run lint` — no new lint errors

## Dev Notes

### Epic Context
[Source: docs/prd-phase2.5/epic-11/index.md]

Story 11.3 is the third story in Epic 11 (Cart & Checkout MVP). It depends on **both Story 11.1** (pricing data + cart calculation functions) and **Story 11.2** (CartSidebar shell + AppShell wiring). It populates the cart sidebar with live content — subkit sections, item rows with quantity controls, subtotals, and a grand total. Story 11.4 (OrderConfirmationScreen) depends on 11.3.

### Previous Story Insights
[Source: docs/stories/11.1.story.md, docs/stories/11.2.story.md]

**Story 11.1** (Item Pricing Data + Cart Calculation Pure Functions) is Done. Key takeaways:
- All 238 tests pass (228 existing + 10 new from 11.1).
- `pricePlaceholder` values are populated on all 28 items in `kitItems.ts`.
- `cartCalculations.ts` created with `CONTAINER_PRICES`, `calculateItemLineTotal`, `calculateSubkitCartTotal`, `calculateCartGrandTotal`.
- No carry-over technical debt.

**Story 11.2** (Cart Icon in AppHeader + CartSidebar Shell + AppShell Wiring) is Approved. Key takeaways:
- `CartSidebar.tsx` exists at `src/components/cart/CartSidebar.tsx` with shell structure (placeholder body + footer).
- `AppShell.tsx` owns `cartOpen` local state, passes `onCartToggle` to AppHeader and `isOpen`/`onClose` to CartSidebar.
- `CartSidebar` has `role="dialog"`, `aria-modal="true"`, `aria-label="Cart"`, focus management, Escape key handling.
- 6 component tests in `tests/components/CartSidebar.test.tsx` — all must continue to pass.

### Current CartSidebar.tsx Structure
[Source: src/components/cart/CartSidebar.tsx]

The current file has:
- Imports: `FC`, `useEffect`, `useRef` from react; `X` from lucide-react
- `CartSidebarProps` interface: `isOpen: boolean`, `onClose: () => void`
- Shell with three sections: header (Cart title + X close), body (placeholder "Your cart is empty."), footer (static "Total" / "$0.00")
- Focus management: `useEffect` focuses panel when `isOpen` becomes true
- Escape key: `useEffect` listens for Escape when `isOpen`
- The body and footer sections are the only parts being replaced in this story — the header row, backdrop, panel wrapper, focus/escape logic remain unchanged

### Key Imports to Add
[Source: src/store/kitStore.ts, src/data/kitItems.ts, src/utils/cartCalculations.ts, src/utils/categoryUtils.ts, src/utils/iconResolver.ts]

```typescript
import { useKitStore } from '../../store/kitStore';
import { ITEMS } from '../../data/kitItems';
import { CATEGORIES } from '../../data/kitItems';
import {
  CONTAINER_PRICES,
  calculateSubkitCartTotal,
  calculateCartGrandTotal,
} from '../../utils/cartCalculations';
import { getCategoryColor, getCategoryIcon } from '../../utils/categoryUtils';  // getCategoryById not needed — use CATEGORIES directly
import { resolveIcon } from '../../utils/iconResolver';
```

### Type References
[Source: src/types/kit.types.ts]

```typescript
export interface SubkitSelection {
  subkitId: string;
  categoryId: string;
  size: 'regular' | 'large';
  selectionOrder: number;
}

export interface ItemSelection {
  itemId: string;
  subkitId: string;
  quantity: number;
  included: boolean;
}

export interface KitItem {
  id: string;
  categoryId: string;
  name: string;
  pricePlaceholder: number | null;
  // ... other fields not used by CartSidebar
}
```

### Store Actions Used
[Source: src/store/kitStore.ts]

- `toggleItem(subkitId: string, itemId: string)`: If the item selection entry exists, it removes the entry entirely from `itemSelections` (deletes the key `${subkitId}::${itemId}`). If it does not exist, it creates a new entry with `{ itemId, subkitId, quantity: 1, included: true }`. Used by the remove (X) button — calling it on an included item removes it from the cart.
- `setItemQuantity(subkitId: string, itemId: string, qty: number)`: Sets the quantity on the item selection. Used by `−` and `+` buttons.

### Category Icon Resolution Pattern
[Source: src/utils/categoryUtils.ts, src/utils/iconResolver.ts]

Category icons are resolved in two steps:
1. `getCategoryIcon(categoryId)` returns the icon name string (e.g., `'Zap'`, `'Lightbulb'`)
2. `resolveIcon(iconName)` maps the string to the actual `LucideIcon` component from the `ICON_MAP`

Render pattern:
```typescript
const iconName = getCategoryIcon(subkit.categoryId);
const IconComponent = iconName ? resolveIcon(iconName) : undefined;
// Then: {IconComponent && <IconComponent size={16} />}
```

### Category Color Lookup
[Source: src/utils/categoryUtils.ts, src/data/kitItems.ts]

```typescript
const colors = getCategoryColor(subkit.categoryId);
const categoryBaseColor = colors?.base ?? '#666';
```

Available category base colors: power=#C2410C, lighting=#A16207, communications=#1D4ED8, hygiene=#0F766E, cooking=#15803D, medical=#991B1B, comfort=#6D28D9, clothing=#334155, custom=#3730A3.

### Category Name Lookup
[Source: src/data/kitItems.ts]

Category names are available via the `CATEGORIES` record:
```typescript
import { CATEGORIES } from '../../data/kitItems';
const categoryName = CATEGORIES[subkit.categoryId]?.name ?? subkit.categoryId;
```

### Selection Key Format
[Source: src/store/kitStore.ts]

All lookups of `itemSelections` use the key format `${subkitId}::${itemId}`. When iterating to find items for a given subkit, filter `Object.values(itemSelections)` by `sel.subkitId === subkit.subkitId` and `sel.included === true` — do not reconstruct keys manually.

### Item Row Sorting
[Source: docs/prd-phase2.5/epic-11/story-113-cartsidebar-live-content.md]

```typescript
const includedItems = Object.values(itemSelections)
  .filter(sel => sel.subkitId === subkit.subkitId && sel.included)
  .sort((a, b) => a.itemId.localeCompare(b.itemId));
```

### Container Price Display
[Source: src/utils/cartCalculations.ts]

`CONTAINER_PRICES` is exported from `cartCalculations.ts`:
```typescript
export const CONTAINER_PRICES: Record<'regular' | 'large', number> = {
  regular: 40,
  large: 60,
};
```
Use `CONTAINER_PRICES[subkit.size]` for the price badge — do NOT hardcode `40` or `60`.

### Currency Formatting
[Source: docs/prd-phase2.5/epic-11/story-113-cartsidebar-live-content.md]

Use `(value).toFixed(2)` for all displayed dollar amounts. Display as `` `$${value.toFixed(2)}` `` string interpolation. The calculation functions return raw numbers — `.toFixed(2)` is the rendering component's responsibility.

### File Locations
[Source: docs/prd-phase2.5/epic-11/story-113-cartsidebar-live-content.md]

| Action | File |
|--------|------|
| Modify | `src/components/cart/CartSidebar.tsx` — replace placeholder body and footer with live content |
| Modify | `tests/components/CartSidebar.test.tsx` — add 9 new test cases per AC11 |

No other files change in this story. All pricing data, calculation functions, and store actions were established in Stories 11.1 and 11.2.

### Compatibility Constraints
[Source: docs/prd-phase2.5/epic-11/compatibility-requirements.md]

- **CC2:** Zustand store shape is unchanged. No new fields on `KitStore`, `KitItem`, `SubkitSelection`, or `ItemSelection`.
- **CC5:** All critical rules in `docs/architecture/11-frontend-developer-standards.md` apply. Dynamic category colors use `style={{ ... }}` — never Tailwind arbitrary values (Rule 8).
- **CC6:** All Phase 2.5 features (`SubkitStatsStrip`, visualizer outer shell, weight/volume stats) are unaffected.

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]

- Named exports only — no `export default`
- `FC<Props>` typed components with co-located `{ComponentName}Props` interface
- No logic in JSX — extract to `const` above return
- TypeScript strict mode must pass (`tsc --noEmit`)
- lucide-react: named imports only (e.g., `import { X } from 'lucide-react'`)
- Dynamic category colors: inline `style={{ borderLeftColor: color }}` — NEVER Tailwind arbitrary values

### Testing

[Source: docs/architecture/9-testing-requirements.md, tests/components/CartSidebar.test.tsx]

- **Test file:** `tests/components/CartSidebar.test.tsx` (extended from Story 11.2)
- **Framework:** Vitest + React Testing Library + vitest-axe
- **axe pattern:**
  ```typescript
  import { axe } from 'vitest-axe';
  import * as matchers from 'vitest-axe/matchers';
  expect.extend(matchers);
  ```
- **Store mocking:** Mock `useKitStore` using `vi.mock('../../src/store/kitStore')`. Provide a minimal mock shape covering `selectedSubkits`, `itemSelections`, `emptyContainers`, `toggleItem: vi.fn()`, `setItemQuantity: vi.fn()`. Set up mock return values per test using `vi.mocked(useKitStore).mockReturnValue(...)`.
- **Existing tests:** The 6 Story 11.2 tests must continue to pass. They test panel open/close state, backdrop click, X button click, ARIA attributes, and axe accessibility — these now need the store mock to return empty/default state.
- **New tests (9 total):** Cover empty state, item rendering, line totals, subtotals, grand total, quantity button actions, remove button action, empty-container rendering, and axe accessibility with populated content.
- Every component test must include at least one axe accessibility assertion.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Initial story draft | Bob, SM Agent |

## QA Results

### Review Date: 2026-03-11

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

The implementation is well-structured and follows the project's established patterns. CartSidebar.tsx is cleanly organized with proper separation of data derivation from JSX, correct use of inline styles for dynamic category colors (Rule 8 compliance), and appropriate Zustand store subscriptions. The test file provides thorough coverage across 15 tests (6 existing + 9 new) with proper store mocking, action verification, and axe accessibility assertions.

One deviation from AC1 was identified regarding the empty state condition logic (see below).

### Requirements Traceability

| AC | Status | Validating Test(s) / Evidence |
|----|--------|-------------------------------|
| AC1 — Empty state message | ✅ PASS | **Given** no subkits selected, **When** CartSidebar renders, **Then** "Your kit is empty" message shown → Test: "renders empty message when no subkits". **Given** all subkits are empty containers with no included items, **When** CartSidebar renders, **Then** "Your kit is empty" message shown → Test: "renders empty state when all selected subkits are empty containers with no included items". Both conditions now covered. |
| AC2 — Store field access | ✅ PASS | CartSidebar.tsx lines 20–21 destructure exactly `selectedSubkits`, `itemSelections`, `emptyContainers`, `toggleItem`, `setItemQuantity`. `ITEMS` and `CATEGORIES` imported from `kitItems.ts`. `calculateSubkitCartTotal` and `calculateCartGrandTotal` imported from `cartCalculations.ts`. |
| AC3 — Subkit header row | ✅ PASS | Icon resolved via `getCategoryIcon` + `resolveIcon` at size 16. Name in 14px/500 (font-medium), neutral-900. Size label + container price in 12px/400, neutral-500. 4px left border via inline `style={{ borderLeftColor }}`. `border-b`, `mb-1` applied. |
| AC4 — Empty-container body | ✅ PASS | `emptyContainers.includes(subkit.subkitId)` check renders "◈ Empty container" in 12px, neutral-400, italic, `py-2 px-1`. No item rows. → Test: "renders empty container text and no quantity controls". |
| AC5 — Item rows | ✅ PASS | Item name with `truncate`, 14px/400, neutral-700. Quantity `−`/`+` buttons with proper disabled states at 1 and 10. `w-6 h-6 rounded-sm` button styling. Line total as `$XX.XX` via `.toFixed(2)`. Remove button with `X` icon (size 12), correct `aria-label`. → Tests: item name, line total, minus button, remove X. |
| AC6 — Subtotal row | ✅ PASS | `calculateSubkitCartTotal` called with correct args. "Subtotal" in 12px/400, neutral-500; value in 14px/500, neutral-700. `pt-2 mt-1 border-t` styling. → Test: "subtotal reflecting container price plus item costs". |
| AC7 — Section separators | ✅ PASS | `<hr>` with `border-[var(--color-neutral-100)] my-3` rendered between sections via `index > 0` check (equivalent to "except after last"). |
| AC8 — Footer grand total | ✅ PASS | "Total" in 14px/500, neutral-700. Grand total in 18px/600 (font-semibold), neutral-900. "Container prices included" in 12px, neutral-400, mt-1. → Test: "grand total equals sum of all subkit subtotals". |
| AC9 — Item row border color | ✅ PASS | Each item row div has `style={{ borderLeftWidth: '4px', borderLeftStyle: 'solid', borderLeftColor: categoryBaseColor }}`. No Tailwind arbitrary values used. |
| AC10 — Live updates | ✅ PASS | CartSidebar reads directly from `useKitStore()`. Zustand subscription model provides automatic re-renders. No polling or event bus. |
| AC11 — Test coverage | ✅ PASS | All 6 Story 11.2 tests pass (store mock returns default empty state). 10 new tests added (9 from AC11 table + 1 for CONCERN-001 fix). Total: 16 tests, 254/254 suite-wide. |

### Compliance Check

- Coding Standards: ✓ Named exports, `FC<Props>`, no logic in JSX (data derived above return), lucide-react named imports
- Project Structure: ✓ No new files, only `CartSidebar.tsx` and `CartSidebar.test.tsx` modified as specified
- Testing Strategy: ✓ Vitest + RTL + vitest-axe, store properly mocked, two axe assertions (empty + populated), no snapshots
- All ACs Met: ✓ All 11 ACs fully met including AC1 empty state fix

### Issue Detail

**CONCERN-001: AC1 empty state incomplete — RESOLVED**
- **Severity:** Medium
- **Status:** Fixed and verified in re-review (2026-03-11)
- **Original finding:** `showEmptyState` only checked `sortedSubkits.length === 0`, missing the "all subkits are empty-container" case.
- **Fix applied:** `CartSidebar.tsx` lines 40–42 now derive `hasIncludedItems` and check both conditions: no subkits OR (no included items AND all subkits are empty containers).
- **Test added:** "renders empty state when all selected subkits are empty containers with no included items" validates the fix. Existing empty-container test adjusted with a second subkit containing an included item to properly isolate AC4 behavior.

### Refactoring Performed

None — no refactoring was performed during this review.

### Security Review

No security concerns. CartSidebar is a read-only display component with no user input beyond quantity controls and remove actions, all of which route through existing Zustand store actions with established validation.

### Performance Considerations

No performance concerns. Category color/icon lookups inside `.map()` are lightweight dictionary lookups. `Object.values(itemSelections).filter().sort()` runs per subkit but item counts are small (max ~28 items). Zustand selectors could optimize re-renders if future profiling shows excess rendering, but this is a low-risk future optimization.

### Files Modified During Review

None.

### Gate Status

Gate: PASS → docs/qa/gates/11.3-cartsidebar-live-content.yml (updated from CONCERNS on re-review)

### Recommended Status

✓ Ready for Done — All acceptance criteria met. CONCERN-001 resolved. 254 tests passing suite-wide, zero typecheck errors, zero lint errors.

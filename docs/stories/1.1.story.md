# Story 1.1: Project Scaffolding and Setup

## Status: Draft

## Story

**As a** developer,
**I want** a fully configured React project with Tailwind CSS, routing, and a deployment pipeline,
**so that** the team has a clean, working foundation to build all features upon.

## Acceptance Criteria

1. React application initialized with TypeScript support
2. Tailwind CSS configured and working with a sample utility class rendered on screen
3. React Router configured with placeholder routes for Kit Builder, Item Configuration, and Summary Page
4. Project deployable to Vercel with a publicly accessible URL
5. Basic README documents how to run, build, and deploy the project locally

## Tasks / Subtasks

- [ ] Task 1: Initialize Vite + React + TypeScript project (AC: 1)
  - [ ] Create project with `npm create vite@latest` using React + TypeScript template
  - [ ] Configure `tsconfig.json` with `strict: true`
  - [ ] Verify `npm run dev` starts without errors
- [ ] Task 2: Install and configure core dependencies (AC: 1, 2, 3)
  - [ ] Install: `react-router-dom@6`, `zustand@5`, `lucide-react`
  - [ ] Install: `tailwindcss@4`, `autoprefixer`, `postcss`
  - [ ] Install dev: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@axe-core/react`, `jsdom`
  - [ ] Install dev: `eslint`, `@typescript-eslint/eslint-plugin`, `eslint-plugin-jsx-a11y`, `prettier`
- [ ] Task 3: Configure Tailwind CSS v4 with design tokens (AC: 2)
  - [ ] Create `src/styles/globals.css` with `@import "tailwindcss"` and `@theme` block containing all design tokens from architecture
  - [ ] Create `src/tokens/design-tokens.ts` as single source of truth for colors, motion values
  - [ ] Import `globals.css` in `src/main.tsx`
  - [ ] Verify a Tailwind utility class renders correctly on screen
- [ ] Task 4: Configure React Router with placeholder routes (AC: 3)
  - [ ] Create `src/router/index.tsx` with `createBrowserRouter` and routes for `/`, `/configure/:subkitId`, `/configure/custom`, `/summary`, and catch-all `*`
  - [ ] Create `src/router/guards.ts` with placeholder guard functions (return null for now)
  - [ ] Create placeholder screen components: `SubkitSelectionScreen.tsx`, `ItemConfigScreen.tsx`, `CustomSubkitScreen.tsx`, `SummaryScreen.tsx`
  - [ ] Create `src/components/layout/AppShell.tsx` as Outlet wrapper with header placeholder
  - [ ] Wire `RouterProvider` in `src/App.tsx`
- [ ] Task 5: Create project structure skeleton (AC: 1)
  - [ ] Create directory structure per architecture: `src/tokens/`, `src/styles/`, `src/types/`, `src/data/`, `src/utils/`, `src/store/`, `src/router/`, `src/components/layout/`, `src/components/ui/`, `src/components/visualizer/`, `src/components/subkit-selection/`, `src/components/item-config/`, `src/components/summary/`, `src/hooks/`, `tests/unit/`, `tests/components/`
  - [ ] Create barrel export `src/types/index.ts`
  - [ ] Create barrel export `src/data/index.ts`
- [ ] Task 6: Configure testing infrastructure (AC: 1)
  - [ ] Create `vitest.config.ts` per architecture spec
  - [ ] Create `tests/setup.ts` with `@testing-library/jest-dom` import and `testIdAttribute` config
  - [ ] Verify `npm run test` runs without errors
- [ ] Task 7: Configure linting and formatting (AC: 1)
  - [ ] Create `.eslintrc.cjs` with TypeScript, React, jsx-a11y plugins
  - [ ] Create `.prettierrc`
  - [ ] Add npm scripts: `dev`, `build`, `preview`, `lint`, `typecheck`, `test`, `test:run`, `test:coverage`
- [ ] Task 8: Configure environment (AC: 4)
  - [ ] Create `.env.example` with `VITE_PURCHASE_URL` placeholder
  - [ ] Create `src/tokens/env.ts` for typed env access
- [ ] Task 9: Create README (AC: 5)
  - [ ] Document: how to install dependencies, run dev server, build, run tests, deploy
- [ ] Task 10: Verify deployment readiness (AC: 4)
  - [ ] Run `npm run build` successfully
  - [ ] Verify static output in `dist/`

## Dev Notes

### Tech Stack
[Source: docs/architecture/2-frontend-tech-stack.md]
- **Build tool:** Vite 6.x
- **Framework:** React 18.x + TypeScript 5.x strict
- **Styling:** Tailwind CSS v4.x with CSS custom properties
- **State management:** Zustand 5.x (installed now, configured in Story 1.2)
- **Routing:** React Router 6.4+ Data Router API
- **Icons:** lucide-react (named imports only)
- **Testing:** Vitest 2.x + React Testing Library 16.x + @axe-core/react 4.x
- **Linting:** ESLint 8.x + @typescript-eslint + eslint-plugin-jsx-a11y 6.x
- **Formatting:** Prettier 3.x
- **Deployment:** Vercel (static SPA hosting)

### Project Structure
[Source: docs/architecture/3-project-structure.md]
Full directory tree defined — see Task 5 for skeleton. Key entry points:
- `src/main.tsx` — React.StrictMode + RouterProvider
- `src/App.tsx` — RouterProvider root
- `index.html` — Vite entry

### Routing Configuration
[Source: docs/architecture/7-routing.md]
- Use `createBrowserRouter` from `react-router-dom`
- Routes: `/` (SubkitSelectionScreen), `/configure/:subkitId` (ItemConfigScreen), `/configure/custom` (CustomSubkitScreen), `/summary` (SummaryScreen), `*` catch-all redirect to `/`
- Guard functions in `src/router/guards.ts` — placeholder stubs for now (guards fully implemented in later stories)
- AppShell wraps all routes via `<Outlet />`

### Styling
[Source: docs/architecture/8-styling-guidelines.md]
- `globals.css` must contain `@import "tailwindcss"` and a `@theme` block with all design tokens (brand colors, neutrals, semantic colors, radii, shadows, motion durations, easings)
- `:focus-visible` and `prefers-reduced-motion` rules must be in globals.css
- Import Inter font from Google Fonts
- Dynamic category colors use inline styles, not Tailwind template literals

### Component Standards
[Source: docs/architecture/4-component-standards.md]
- Named exports only — no `export default`
- `FC<Props>` typed components
- Props interface `{ComponentName}Props` co-located in same file
- No logic in JSX — extract to `const` above return
- PascalCase `.tsx` files, camelCase `.ts` for utils/hooks

### Environment
[Source: docs/architecture/10-environment-configuration.md]
- Static SPA — no backend, no secrets in MVP
- Single env var: `VITE_PURCHASE_URL` (placeholder)
- Typed access via `src/tokens/env.ts` — never use `import.meta.env` directly in components

### Brownfield Context
[Source: docs/architecture/1-template-and-framework-selection.md]
- This is a brownfield refactor. Story 1.1 establishes the target scaffold. Existing files are corrected in later stories where logically required.
- Pre-refactor tag (`v0-pre-refactor`) should be created before any story begins.

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test location: `tests/unit/` for unit tests, `tests/components/` for component tests
- Framework: Vitest 2.x + React Testing Library 16.x
- `vitest.config.ts` uses jsdom environment, globals: true, setup file at `tests/setup.ts`
- `tests/setup.ts` imports `@testing-library/jest-dom` and configures `testIdAttribute: 'data-testid'`
- No snapshot tests allowed
- Every component test file must include at least one axe accessibility assertion

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |

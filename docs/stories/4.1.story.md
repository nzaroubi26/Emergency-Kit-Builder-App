# Story 4.1: Subkit Item Configuration Screen

## Status: Draft

## Story

**As a** user,
**I want** to see all available items for each of my selected subkits and choose what to include,
**so that** I can build a kit that reflects exactly what I need without duplicating things I already own.

## Acceptance Criteria

1. The Item Configuration screen displays one subkit at a time in a clear, focused layout
2. All predefined items for the current subkit are listed with their name and a brief description
3. Each item has an include/exclude toggle — all items excluded by default
4. A progress indicator shows which subkit is being configured and how many remain
5. The current subkit assigned color is used as a visual accent for continuity
6. The user can navigate back to the previous subkit without losing selections
7. Item selections and quantities are preserved as the user moves between subkits

## Tasks / Subtasks

- [ ] Task 1: Implement ItemConfigScreen layout (AC: 1, 2, 5)
  - [ ] Update `src/components/item-config/ItemConfigScreen.tsx`
  - [ ] Read current subkit from route param `:subkitId` via `useParams()`
  - [ ] Resolve category data from `CATEGORIES[subkitId]`
  - [ ] Display heading: "Configure Your {Category Name} Subkit" with category color accent
  - [ ] Focus heading on mount per focus management pattern
  - [ ] List items from `ITEMS_BY_CATEGORY[subkitId]`
- [ ] Task 2: Create ItemCard component (AC: 2, 3)
  - [ ] Create `src/components/item-config/ItemCard.tsx`
  - [ ] Display: item image (ImageWithFallback), name, description
  - [ ] Include/exclude toggle — all items excluded by default
  - [ ] On toggle: call `toggleItem(subkitId, itemId)` from store
  - [ ] Excluded state: neutral, dimmed
  - [ ] Included state: highlighted with category color accent
  - [ ] Grid layout for item cards
- [ ] Task 3: Create ImageWithFallback component (AC: 2)
  - [ ] Create/correct `src/components/ui/ImageWithFallback.tsx`
  - [ ] Fallback: category tint background + centered category icon (32px) + bottom gradient overlay
  - [ ] All items use fallback in MVP (imageSrc is null)
  - [ ] Use inline style for dynamic category tint color
- [ ] Task 4: Create SubkitProgressIndicator (AC: 4)
  - [ ] Create `src/components/item-config/SubkitProgressIndicator.tsx`
  - [ ] Display: "Subkit N of M" label + progress bar
  - [ ] Progress bar filled portion uses category color
  - [ ] N = current position, M = total selected subkits
- [ ] Task 5: Implement back navigation (AC: 6, 7)
  - [ ] Back button navigates to previous subkit in sequence
  - [ ] If first subkit: back navigates to SubkitSelectionScreen with confirmation modal
  - [ ] All item selections preserved in store across navigation
- [ ] Task 6: Create ConfirmationModal component (AC: 6)
  - [ ] Create `src/components/ui/ConfirmationModal.tsx`
  - [ ] Focus-trapped dialog per architecture
  - [ ] Escape key = cancel only (does not confirm)
  - [ ] Message for back-to-selection: "Going back will keep all your selections. You can return at any time."
- [ ] Task 7: Write component tests (AC: 2, 3)
  - [ ] Create `tests/components/ItemCard.test.tsx`
  - [ ] Test: renders item name and description
  - [ ] Test: toggle switches between included/excluded
  - [ ] Test: excluded by default
  - [ ] Include axe accessibility assertion

## Dev Notes

### Brownfield Correction: ItemQuestionnaireFlow.tsx
[Source: docs/architecture/1-template-and-framework-selection.md]
- `ItemQuestionnaireFlow.tsx` is corrected in Epic 4 — replaced by `ItemConfigScreen.tsx`
- Key corrections: remove dual volume bars, add EmptyContainerOption (Story 4.3), cap quantity at 10 (Story 4.2), replace free-form Custom with category browser (Story 4.4)

### Brownfield Correction: ImageWithFallback
[Source: docs/architecture/1-template-and-framework-selection.md]
- Corrected in Epic 4 (first screen to render item images)
- Fallback = category tint bg + centered category icon 32px + bottom gradient overlay

### Store Actions
[Source: docs/architecture/5-state-management.md#store]
- `toggleItem(subkitId, itemId)`: toggles item in/out of `itemSelections` record
- Key format: `${subkitId}::${itemId}`
- If subkit is in `emptyContainers`, toggleItem is blocked
- Default quantity on first include: 1

### QuantitySelector Layout Rule
[Source: docs/architecture/11-frontend-developer-standards.md, Rule 8]
- QuantitySelector container always reserves its layout space
- Only opacity and pointer-events change — never conditionally render or display:none
- Prevents CLS (Cumulative Layout Shift)

### Confirmation Modal
[Source: UX Spec Section 2.2]
- Back to Subkit Selection: "Going back will keep all your selections. You can return at any time."
- Focus-trapped, Escape = cancel only

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test file: `tests/components/ItemCard.test.tsx`
- Test behavior with getByRole, getByText
- axe assertion required
- Reset store in beforeEach

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |

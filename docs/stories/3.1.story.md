# Story 3.1: Subkit Category Cards

## Status: Done

## Story

**As a** user,
**I want** to browse and select subkit categories from a clear, visual set of options,
**so that** I can quickly identify and choose the subkits that best fit my household needs.

## Acceptance Criteria

1. All 9 subkit categories are displayed as individual selectable cards
2. Each card displays the subkit name, icon, and a brief one-line description
3. Selected cards are visually distinguished from unselected cards
4. Each card reflects the subkit assigned color consistently with the visualizer
5. Cards are arranged in a clean, scannable grid layout on desktop and tablet
6. The Custom subkit card is visually distinguishable as a flexible open option

## Tasks / Subtasks

- [ ] Task 1: Create SubkitCard component (AC: 1, 2, 3, 4)
  - [ ] Create `src/components/subkit-selection/SubkitCard.tsx`
  - [ ] Props: category data, selected state, disabled state, onSelect callback
  - [ ] Display: category icon (lucide-react named import), name, description
  - [ ] Default state: white bg, neutral border
  - [ ] Selected state: category `colorTint` bg, category `colorBase` border (2px), checkmark indicator
  - [ ] Disabled state: `opacity-45 cursor-not-allowed`
  - [ ] Use inline style for dynamic category colors, Tailwind for structural styling
  - [ ] `role="button"`, `tabIndex={0}`, `aria-pressed={selected}`, `aria-label`
  - [ ] Add `className="subkit-card"` for focus-visible override in globals.css
- [ ] Task 2: Create SubkitSelectionScreen layout (AC: 1, 5)
  - [ ] Update `src/components/subkit-selection/SubkitSelectionScreen.tsx`
  - [ ] Layout: HousingUnitVisualizer at top, slot counter, then card grid below
  - [ ] Grid: 3 columns desktop, 2 columns tablet (responsive via Tailwind grid)
  - [ ] Heading: "Build Your Kit" + subheading "Choose 3-6 categories for your kit"
  - [ ] Focus heading on mount per focus management pattern
- [ ] Task 3: Wire cards to store (AC: 1, 3)
  - [ ] Map over all categories from `CATEGORIES` to render cards
  - [ ] On card click: call `selectSubkit(categoryId)` or `deselectSubkit(subkitId)` based on current state
  - [ ] Derive selected state from `useKitStore(s => s.selectedSubkits)`
  - [ ] Derive disabled state from `useIsAtCapacity()`
- [ ] Task 4: Custom card distinction (AC: 6)
  - [ ] Custom card uses `Settings2` icon and its unique color (`#3730A3`)
  - [ ] Add subtle visual distinction — e.g., dashed border or badge indicating "mix & match"
- [ ] Task 5: Write component tests (AC: 1, 2, 3, 4)
  - [ ] Create `tests/components/SubkitCard.test.tsx`
  - [ ] Test: renders name, icon, description
  - [ ] Test: selected state shows correct styling
  - [ ] Test: disabled state applies correct classes
  - [ ] Test: click calls onSelect
  - [ ] Include axe accessibility assertion

## Dev Notes

### Brownfield Context
[Source: docs/architecture/1-template-and-framework-selection.md]
- `SubkitTypeSelectionNew.tsx` is corrected in Epic 3 — this story replaces it with `SubkitSelectionScreen.tsx`
- Key corrections: top-to-bottom fill, >= 3 minimum, 9-category colors, slot-based constraint, opacity-45 disabled state

### Component Standards
[Source: docs/architecture/4-component-standards.md]
- Named export only, `FC<SubkitCardProps>` typed
- No logic in JSX — extract to const above return
- Props interface co-located in same file

### Dynamic Colors
[Source: docs/architecture/11-frontend-developer-standards.md, Rule 3]
- Card border/background colors MUST use inline styles: `style={{ borderColor: category.colorBase }}`
- Never use Tailwind template literal classes for dynamic hex values

### Icon Import
[Source: docs/architecture/11-frontend-developer-standards.md, Rule 7]
- Named imports only from lucide-react: `import { Zap, Lightbulb, Radio, ... } from 'lucide-react'`
- Use category `icon` field to dynamically resolve icon component

### Focus Visible
[Source: docs/architecture/8-styling-guidelines.md]
- `.subkit-card:focus-visible` has custom outline: 3px solid brand-primary, offset 0

### Category Data
[Source: docs/architecture/6-data-layer.md]
All 9 categories with icons: Power(Zap), Lighting(Lightbulb), Communications(Radio), Hygiene(Droplets), Cooking(UtensilsCrossed), Medical(HeartPulse), Comfort(Smile), Clothing(Shirt), Custom(Settings2)

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test file: `tests/components/SubkitCard.test.tsx`
- Test behavior with getByRole, getByText — not class names
- axe assertion required

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |

# Story 11.2: Cart Icon in AppHeader + CartSidebar Shell + AppShell Wiring

## Status: Approved

## Story

**As a** user,
**I want** to see a shopping cart icon in the app header that opens a collapsible sidebar panel,
**so that** I know my cart is available at any point during kit configuration and can open it to review what I have selected.

## Acceptance Criteria

1. **AC1 — AppShell owns `cartOpen` boolean local state.**
   `cartOpen` is initialized to `false`. It is never stored in Zustand. AppShell provides `onCartToggle` (toggles open/close) to AppHeader and `isOpen` + `onClose` to CartSidebar. No other component owns or duplicates this state.

2. **AC2 — AppShell suppresses CartSidebar on `/confirmation`.**
   AppShell uses `const location = useLocation()` from `react-router-dom`. When `location.pathname === '/confirmation'`, `CartSidebar` is not rendered at all (conditional render, not CSS hide). On all other routes, `CartSidebar` is rendered with `isOpen={cartOpen}` and `onClose={() => setCartOpen(false)}`.

3. **AC3 — AppHeader receives `onCartToggle: () => void` as a new prop.**
   AppHeader renders a `<button>` in its top-right area containing the `ShoppingCart` icon from `lucide-react` (size 22, named import). Clicking the button calls `onCartToggle`. The button has `aria-label="Open cart"`.

4. **AC4 — AppHeader displays a live item-count badge on the cart icon.**
   AppHeader reads `itemSelections` from `useKitStore`. The badge count is computed as:
   ```typescript
   const cartItemCount = Object.values(itemSelections)
     .filter(sel => sel.included)
     .reduce((sum, sel) => sum + sel.quantity, 0);
   ```
   The badge renders as a small circle overlaid on the top-right of the ShoppingCart icon. It is visible only when `cartItemCount > 0`. Badge styles: `absolute -top-1.5 -right-1.5`, min-width `w-4 h-4`, `rounded-full`, `bg-[var(--color-brand-primary)]`, white text, `text-[10px] font-medium`, centered. When `cartItemCount` is 0 the badge element is not rendered (conditional render).

5. **AC5 — `CartSidebar` component created at `src/components/cart/CartSidebar.tsx`.**
   Props interface:
   ```typescript
   interface CartSidebarProps {
     isOpen: boolean;
     onClose: () => void;
   }
   ```
   The component is a named export: `export const CartSidebar: FC<CartSidebarProps> = ...`

6. **AC6 — CartSidebar renders as a fixed right-side panel.**
   When `isOpen` is `true`, the panel is visible at the right edge of the viewport. When `isOpen` is `false`, it is fully off-screen to the right. Structure:
   - Outermost wrapper: `position: fixed`, `inset-y-0 right-0`, `w-80` (320px), `bg-white`, `shadow-3` (20px shadow per design tokens), `z-50`, `flex flex-col`
   - Slide transform: `translate-x-0` (open) / `translate-x-full` (closed)
   - Transition applied via inline `style={{ transition: 'transform 240ms cubic-bezier(0.4,0,0.2,1)' }}`
   - The panel is always present in the DOM when CartSidebar is mounted (it does not unmount when closed) — the slide transition requires the element to exist

7. **AC7 — Backdrop renders behind the open panel.**
   When `isOpen` is `true`, a backdrop `div` renders with:
   - `position: fixed`, `inset-0`, `bg-black/40`, `z-40`
   - Inline `style={{ transition: 'opacity 200ms cubic-bezier(0.4,0,0.2,1)' }}`
   - `onClick={onClose}`, `aria-hidden="true"`
   When `isOpen` is `false`, the backdrop is not rendered (conditional render).

8. **AC8 — CartSidebar shell structure.**
   The panel interior has three vertical sections (`flex flex-col h-full`):

   *Header row* (`flex-shrink-0`, `flex items-center justify-between`, `px-4 py-3`, `border-b border-[var(--color-neutral-200)]`):
   - Left: heading `"Cart"` in `text-h3` (18px / 600), `neutral-900`
   - Right: close button — `X` icon from lucide-react (size 20), `aria-label="Close cart"`, calls `onClose`

   *Body* (`flex-1 overflow-y-auto px-4 py-3`):
   - Story 11.2 renders a placeholder: a single centered `<p>` reading `"Your cart is empty."` in `text-body`, `neutral-400`, centered. This placeholder is replaced entirely in Story 11.3.

   *Footer* (`flex-shrink-0`, `border-t border-[var(--color-neutral-200)]`, `px-4 py-3`):
   - Story 11.2 renders a single row: `"Total"` label left, `"$0.00"` right, both in `text-label` (14px / 500), `neutral-700`. This footer is replaced in Story 11.3.

9. **AC9 — Accessibility.**
   - CartSidebar root element: `role="dialog"`, `aria-modal="true"`, `aria-label="Cart"`, `tabIndex={-1}`
   - When `isOpen` transitions from `false` to `true`, focus moves to the CartSidebar root element via `useEffect` + `ref.current?.focus()`
   - When the sidebar closes, focus returns to the cart icon button in AppHeader — pass a `cartButtonRef` from AppShell to AppHeader, and call `cartButtonRef.current?.focus()` in the `onClose` handler after `setCartOpen(false)`
   - Keyboard: pressing `Escape` while the sidebar is open calls `onClose`. Handle via `useEffect` keydown listener on the document inside CartSidebar, active only when `isOpen` is `true`.
   - The `ShoppingCart` button in AppHeader has `aria-expanded={cartOpen}` to communicate sidebar state to screen readers.

10. **AC10 — Component tests.**
    `tests/components/CartSidebar.test.tsx` covers:
    - Sidebar panel not visible (off-screen) when `isOpen={false}`
    - Sidebar panel visible when `isOpen={true}`
    - Clicking backdrop calls `onClose`
    - Clicking X button calls `onClose`
    - `role="dialog"` and `aria-label="Cart"` present on panel root
    - axe-core accessibility assertion passes with `isOpen={true}`

## Tasks / Subtasks

- [ ] Task 1: Create `src/components/cart/CartSidebar.tsx` (AC: 5, 6, 7, 8, 9)
  - [ ] Create directory `src/components/cart/`
  - [ ] Create `CartSidebar.tsx` with `CartSidebarProps` interface (`isOpen: boolean`, `onClose: () => void`)
  - [ ] Implement panel as fixed right-side `div` with `role="dialog"`, `aria-modal="true"`, `aria-label="Cart"`, `tabIndex={-1}`
  - [ ] Apply slide transform: `translate-x-0` when open, `translate-x-full` when closed
  - [ ] Apply inline `style={{ boxShadow: '0 20px 25px rgba(0,0,0,0.12)', transition: 'transform 240ms cubic-bezier(0.4,0,0.2,1)' }}`
  - [ ] Implement backdrop: conditionally rendered `div` when `isOpen` is `true`, with `fixed inset-0 bg-black/40 z-40`, `onClick={onClose}`, `aria-hidden="true"`
  - [ ] Add header row: "Cart" heading (18px / 600, neutral-900) left, X close button (size 20, `aria-label="Close cart"`) right, border-bottom
  - [ ] Add body section: placeholder `<p>` "Your cart is empty." centered, neutral-400
  - [ ] Add footer section: "Total" left, "$0.00" right, border-top, text-label (14px / 500), neutral-700
  - [ ] Add `useEffect` for focus management: focus panel ref when `isOpen` transitions to `true`
  - [ ] Add `useEffect` for Escape key listener: active only when `isOpen` is `true`, calls `onClose`

- [ ] Task 2: Modify `src/components/layout/AppHeader.tsx` (AC: 3, 4, 9)
  - [ ] Extend `AppHeaderProps` interface: add `onCartToggle: () => void`, `cartOpen?: boolean`, `cartButtonRef?: React.RefObject<HTMLButtonElement>`
  - [ ] Add import: `ShoppingCart` from `lucide-react` (named import on existing lucide-react line if present, otherwise new import)
  - [ ] Add import: `useKitStore` from `../../hooks/useKitStore`
  - [ ] Read `itemSelections` from `useKitStore()` inside the component
  - [ ] Compute `cartItemCount` from `itemSelections`: filter `sel.included`, reduce to sum of `sel.quantity`
  - [ ] Append cart button to the right side of the existing flex row (after `StepProgressIndicator`), with `relative` positioning wrapper
  - [ ] Cart button: `ShoppingCart` icon (size 22), `aria-label="Open cart"`, `aria-expanded={cartOpen}`, `onClick={onCartToggle}`, `ref={cartButtonRef}`
  - [ ] Badge overlay: conditionally rendered when `cartItemCount > 0`, `absolute -top-1.5 -right-1.5 w-4 h-4 rounded-full bg-[var(--color-brand-primary)] text-white text-[10px] font-medium flex items-center justify-center`

- [ ] Task 3: Modify `src/components/layout/AppShell.tsx` (AC: 1, 2, 9)
  - [ ] Add imports: `useState` from `react`, `useLocation` from `react-router-dom`, `CartSidebar` from `../cart/CartSidebar`
  - [ ] Add `useRef<HTMLButtonElement>(null)` for `cartButtonRef`
  - [ ] Add `const [cartOpen, setCartOpen] = useState(false)` inside component body
  - [ ] Add `const location = useLocation()` and `const isConfirmation = location.pathname === '/confirmation'`
  - [ ] Create `handleCartClose` function: sets `cartOpen` to `false`, then calls `cartButtonRef.current?.focus()`
  - [ ] Pass `onCartToggle={() => setCartOpen(v => !v)}`, `cartOpen={cartOpen}`, and `cartButtonRef={cartButtonRef}` to `<AppHeader>`
  - [ ] Render `CartSidebar` conditionally: `{!isConfirmation && <CartSidebar isOpen={cartOpen} onClose={handleCartClose} />}` — place directly before `<Outlet />`

- [ ] Task 4: Create `tests/components/CartSidebar.test.tsx` (AC: 10)
  - [ ] Import `render`, `screen`, `fireEvent` from `@testing-library/react`
  - [ ] Import `describe`, `it`, `expect` from `vitest`
  - [ ] Import `axe` from `vitest-axe` and extend matchers (follow `StarRating.test.tsx` pattern)
  - [ ] Import `CartSidebar` from `../../src/components/cart/CartSidebar`
  - [ ] Test: panel has `translate-x-full` class when `isOpen={false}`
  - [ ] Test: panel does not have `translate-x-full` class when `isOpen={true}`
  - [ ] Test: clicking backdrop calls `onClose`
  - [ ] Test: clicking X button calls `onClose`
  - [ ] Test: `role="dialog"` and `aria-label="Cart"` present on panel root
  - [ ] Test: axe accessibility assertion passes with `isOpen={true}`

- [ ] Task 5: Run full verification suite (AC: all)
  - [ ] Run `npm run typecheck` — zero errors
  - [ ] Run `npm run test:run` — all new and existing tests green
  - [ ] Run `npm run lint` — no new lint errors

## Dev Notes

### Epic Context
[Source: docs/prd-phase2.5/epic-11/index.md]

Story 11.2 is the second story in Epic 11 (Cart & Checkout MVP). It has **no dependency on Story 11.1** — it creates the cart UI shell (sidebar + header icon) that Story 11.3 will populate with live content. Story 11.3 depends on both 11.1 (pricing functions) and 11.2 (cart shell).

### Previous Story Insights
[Source: docs/stories/11.1.story.md]

Story 11.1 (Item Pricing Data + Cart Calculation Pure Functions) is Done. Key takeaways:
- All 238 tests pass (228 existing + 10 new from 11.1).
- `pricePlaceholder` values are now populated on all 28 items in `kitItems.ts`.
- `cartCalculations.ts` created with `CONTAINER_PRICES`, `calculateItemLineTotal`, `calculateSubkitCartTotal`, `calculateCartGrandTotal`.
- No carry-over technical debt affecting this story.

### Current Source Files
[Source: src/components/layout/AppShell.tsx, src/components/layout/AppHeader.tsx]

**AppShell.tsx** current structure:
- Imports: `FC`, `useEffect`, `useRef` from react; `Outlet` from react-router-dom; `AppHeader`, `MobileInterstitial`, `useIsMobile`, `initAnnouncer`, `injectGA4Script`
- Has two `useEffect` hooks: one for GA4, one for announcer init
- JSX: `<div>` → `<AppHeader />` → `<main>` → `{isMobile ? MobileInterstitial : Outlet}` → two ARIA live regions

**AppHeader.tsx** current structure:
- Imports: `FC` from react; `StepProgressIndicator`
- Empty `AppHeaderProps` interface
- JSX: `<header>` → `<div className="flex ... justify-between">` → `<span>` (title) + `<StepProgressIndicator />`
- The cart button should be placed after `StepProgressIndicator` in the flex row

### Component Specifications
[Source: docs/prd-phase2.5/epic-11/story-112-cart-icon-in-appheader-cartsidebar-shell-appshell-wiring.md]

**CartSidebar component template:**
```typescript
import { type FC, useEffect, useRef } from 'react';
import { X } from 'lucide-react';

interface CartSidebarProps {
  isOpen: boolean;
  onClose: () => void;
}

export const CartSidebar: FC<CartSidebarProps> = ({ isOpen, onClose }) => {
  const panelRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (isOpen) panelRef.current?.focus();
  }, [isOpen]);

  useEffect(() => {
    if (!isOpen) return;
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    document.addEventListener('keydown', handleKeyDown);
    return () => document.removeEventListener('keydown', handleKeyDown);
  }, [isOpen, onClose]);

  return (
    <>
      {isOpen && (
        <div
          className="fixed inset-0 bg-black/40 z-40"
          style={{ transition: 'opacity 200ms cubic-bezier(0.4,0,0.2,1)' }}
          onClick={onClose}
          aria-hidden="true"
        />
      )}
      <div
        ref={panelRef}
        role="dialog"
        aria-modal="true"
        aria-label="Cart"
        tabIndex={-1}
        className={`fixed inset-y-0 right-0 w-80 bg-white z-50 flex flex-col focus:outline-none ${
          isOpen ? 'translate-x-0' : 'translate-x-full'
        }`}
        style={{
          boxShadow: '0 20px 25px rgba(0,0,0,0.12)',
          transition: 'transform 240ms cubic-bezier(0.4,0,0.2,1)',
        }}
      >
        {/* Header */}
        {/* Body (placeholder in Story 11.2) */}
        {/* Footer (placeholder in Story 11.2) */}
      </div>
    </>
  );
};
```

**Shadow value:** `shadow-3` is defined as `0 20px 25px rgba(0,0,0,0.12)` in the design system. Apply via inline `style` since Tailwind arbitrary shadow values are not used per project patterns. [Source: src/tokens/design-tokens.ts]

**Focus management pattern:** Follow the existing `ItemConfigScreen` pattern — `useRef<HTMLDivElement>(null)` + `useEffect(() => { if (isOpen) ref.current?.focus(); }, [isOpen])`. [Source: docs/architecture/11-frontend-developer-standards.md#focus-management-and-aria]

**Focus return on close:** `cartButtonRef` is created in AppShell, passed to AppHeader (which assigns it to the ShoppingCart button), and used in `handleCartClose` to return focus: `cartButtonRef.current?.focus()`.

### File Locations
[Source: docs/architecture/3-project-structure.md, docs/prd-phase2.5/epic-11/story-112]

| Action | File |
|--------|------|
| Create | `src/components/cart/CartSidebar.tsx` |
| Create | `tests/components/CartSidebar.test.tsx` |
| Modify | `src/components/layout/AppShell.tsx` |
| Modify | `src/components/layout/AppHeader.tsx` |

### Compatibility Constraints
[Source: docs/prd-phase2.5/epic-11/compatibility-requirements.md]

- **CC2:** Zustand store shape is unchanged. No new fields on `KitStore`, `KitItem`, `SubkitSelection`, or `ItemSelection`.
- **CC4:** CartSidebar open/closed state is `AppShell` local component state. It is never hoisted into Zustand.
- **CC5:** All critical rules in `docs/architecture/11-frontend-developer-standards.md` apply. Dynamic category colors use `style={{ ... }}` — never Tailwind arbitrary values.
- **CC6:** All Phase 2.5 features (`SubkitStatsStrip`, visualizer outer shell, weight/volume stats) are unaffected.

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]

- Named exports only — no `export default`
- `FC<Props>` typed components with co-located `{ComponentName}Props` interface
- No logic in JSX — extract to `const` above return
- TypeScript strict mode must pass (`tsc --noEmit`)
- lucide-react: named imports only (e.g., `import { ShoppingCart, X } from 'lucide-react'`)

### Router Context
[Source: src/router/index.tsx]

There is no `/confirmation` route yet — it will be added in Story 11.4. The `isConfirmation` check in AppShell should still be implemented now (AC2) so that CartSidebar suppression is ready when the route is added. Current routes under AppShell: `/builder`, `/configure/custom`, `/configure/:subkitId`, `/summary`.

### Testing

[Source: docs/architecture/9-testing-requirements.md, tests/components/StarRating.test.tsx]

- **Test file:** `tests/components/CartSidebar.test.tsx`
- **Framework:** Vitest + React Testing Library + vitest-axe
- **axe pattern:** Follow `StarRating.test.tsx`:
  ```typescript
  import { axe } from 'vitest-axe';
  import * as matchers from 'vitest-axe/matchers';
  expect.extend(matchers);
  ```
- **No MemoryRouter wrapper needed** — CartSidebar does not access router context in Story 11.2
- **Test closed state:** Check `classList.contains('translate-x-full')` on the dialog element
- **Test open state:** Check `translate-x-full` is NOT in classList when `isOpen={true}`
- **Test backdrop click:** Use `fireEvent.click` on the backdrop element (query by `aria-hidden="true"`)
- **Test X button:** Query by `aria-label="Close cart"` and fire click
- **Test accessibility:** Run `axe(container)` and assert `toHaveNoViolations()` with `isOpen={true}`
- Every component test must include at least one axe accessibility assertion

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Initial story draft | Bob, SM Agent |

# Story 6.3: Automated E2E Test Suite (Playwright)

## Status: Done

## Story

**As a** developer,
**I want** automated end-to-end tests covering the three critical user flows,
**so that** Phase 2 and future changes can be validated without full manual regression.

## Acceptance Criteria

1. Playwright is added to `devDependencies` with a `playwright.config.ts` at the project root targeting `http://localhost:5000`
2. E2E tests live in `tests/e2e/` and do not conflict with existing Vitest unit and component tests
3. A `npm run test:e2e` script is added to `package.json`
4. The following three flows are covered with passing tests:
   - **Full configuration flow:** Select 3 subkits, configure items, reach Summary Page, verify kit summary displays all selected subkits and items correctly
   - **Back-navigation state preservation:** Configure 2 subkits, navigate back to Subkit Selection, verify both selections and sizes are preserved in the visualizer and cards
   - **Start Over reset:** Complete configuration, click Start Over, confirm via modal, verify store resets and visualizer shows 6 empty slots
5. All three E2E tests pass in CI

## Tasks / Subtasks

- [x] Task 1: Add Playwright to project (AC: 1, 2)
  - [x] Add Playwright to devDependencies
  - [x] Create `playwright.config.ts` at project root targeting `http://localhost:5000`
  - [x] Configure Chromium project
  - [x] Add webServer config for dev server
  - [x] Ensure tests/e2e/ directory does not conflict with Vitest
- [x] Task 2: Add test:e2e script (AC: 3)
  - [x] Add `test:e2e` script to `package.json`
- [x] Task 3: Write E2E test — Full configuration flow (AC: 4)
  - [x] Select 3 subkits and configure items
  - [x] Navigate to Summary Page
  - [x] Verify kit summary displays all selected subkits and items correctly
- [x] Task 4: Write E2E test — Back-navigation state preservation (AC: 4)
  - [x] Configure 2 subkits
  - [x] Navigate back to Subkit Selection
  - [x] Verify selections and sizes are preserved in visualizer and cards
- [x] Task 5: Write E2E test — Start Over reset (AC: 4)
  - [x] Complete configuration
  - [x] Click Start Over and confirm via modal
  - [x] Verify store resets and visualizer shows 6 empty slots
- [x] Task 6: CI workflow (AC: 5)
  - [x] Create `.github/workflows/e2e.yml` CI workflow for Playwright
  - [x] Verify all three E2E tests pass in CI

## Dev Notes

### Playwright Configuration
- `playwright.config.ts` targets `http://localhost:5000` with Chromium project
- webServer config starts the dev server automatically for E2E runs
- Tests located in `tests/e2e/` — separated from Vitest unit/component tests

### E2E Test Structure
- All tests in `tests/e2e/kit-builder.spec.ts`
- `beforeEach` clears `localStorage` to ensure clean state per test
- Tests exercise the full application through the browser, validating real user flows

### Integration Verification
- IV1: `npm run test:run` (Vitest suite) continues to pass after Playwright is added
- IV2: `npm run test:e2e` completes successfully on a clean checkout with no pre-existing localStorage

## Dev Agent Record

### Files Touched
- `playwright.config.ts` — Playwright config targeting localhost:5000, Chromium project, webServer config
- `tests/e2e/kit-builder.spec.ts` — 3 test flows: full config, back-nav, start over
- `package.json` — test:e2e script added
- `.github/workflows/e2e.yml` — CI workflow for Playwright

## QA Results

Retroactively documented — implementation verified by PO assessment 2026-03-05

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-05 | 1.0 | Retroactive story creation — implementation complete | SM Agent |

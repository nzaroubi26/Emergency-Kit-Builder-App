# Story 6.4: Cover / Landing Page

## Status: Done

## Story

**As a** prospective user,
**I want** to land on a branded introduction page before entering the kit builder,
**so that** I understand the product's value proposition before I start configuring my kit.

## Acceptance Criteria

1. A new `CoverScreen` component is created at `src/components/cover/CoverScreen.tsx` using brand colors (`--color-brand-primary`, `--color-brand-accent`) and the Inter typeface.
2. The cover page is mounted at route `/`. The Subkit Selection Screen moves to route `/builder`.
3. All existing router guards (`subkitConfigGuard`, `customConfigGuard`, `summaryGuard`) are updated to redirect to `/builder` instead of `/`.
4. The cover page includes a headline, a brief value proposition statement, and a single `PrimaryButton` CTA labeled "Build My Kit" that navigates to `/builder`.
5. The cover page has no dependency on the Zustand store — it is fully static.
6. The cover page meets the NFR7 page load target (<=3 seconds on standard broadband).
7. Navigating directly to `/builder`, `/configure/:subkitId`, `/configure/custom`, or `/summary` continues to work correctly under the updated routing.

## Tasks / Subtasks

- [x] Task 1: Create CoverScreen component (AC: 1, 4, 5)
  - [x] New file at `src/components/cover/CoverScreen.tsx`
  - [x] Brand colors: `--color-brand-primary` (#1F4D35), `--color-brand-accent` (#22C55E)
  - [x] Inter typeface consistent with existing screens
  - [x] Headline and value proposition messaging
  - [x] `PrimaryButton` CTA labeled "Build My Kit"
  - [x] Link navigates to `/builder`
  - [x] No Zustand store dependency — fully static component
- [x] Task 2: Update router configuration (AC: 2, 7)
  - [x] `/` renders `CoverScreen`
  - [x] `/builder` renders `SubkitSelectionScreen`
  - [x] Existing routes (`/configure/:subkitId`, `/configure/custom`, `/summary`) unchanged
- [x] Task 3: Update router guards (AC: 3, 7)
  - [x] `subkitConfigGuard` redirects to `/builder` instead of `/`
  - [x] `customConfigGuard` redirects to `/builder` instead of `/`
  - [x] `summaryGuard` redirects to `/builder` instead of `/`
- [x] Task 4: Verify performance and routing (AC: 6, 7)
  - [x] Cover page meets NFR7 page load target (<=3 seconds)
  - [x] Direct navigation to `/builder`, `/configure/:subkitId`, `/configure/custom`, `/summary` works correctly

## Dev Notes

### CoverScreen Component
[Source: docs/prd-phase2.md — Story 6.4]
- Fully static component — no Zustand store dependency
- Uses existing `PrimaryButton` component for CTA
- Brand colors from design tokens: `--color-brand-primary` (#1F4D35), `--color-brand-accent` (#22C55E)
- Inter typeface consistent with existing application styling

### Route Restructure
[Source: docs/architecture/7-routing.md, docs/prd-phase2.md — FR1, FR2]
- `/` → CoverScreen (new)
- `/builder` → SubkitSelectionScreen (moved from `/`)
- All other routes unchanged

### Guard Updates
[Source: docs/prd-phase2.md — CR1]
- All three guards redirect to `/builder` instead of `/`
- Guards continue to validate store state before allowing navigation

### Testing
[Source: docs/architecture/9-testing-requirements.md]
- Integration verification: CoverScreen renders at `/`, CTA navigates to `/builder`
- All existing guard redirects resolve to `/builder`
- Full kit configuration flow from `/builder` through `/summary` completes without regression

## Dev Agent Record

### Files Modified
- `src/components/cover/CoverScreen.tsx` — Brand messaging, Link to /builder
- `src/router/index.tsx` — `/` renders CoverScreen, `/builder` renders SubkitSelectionScreen
- `src/router/guards.ts` — subkitConfigGuard, customConfigGuard, summaryGuard all redirect to /builder

### QA Results
Retroactively documented — implementation verified by PO assessment 2026-03-05

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-05 | 1.0 | Retroactive story creation — implementation complete | SM Agent |

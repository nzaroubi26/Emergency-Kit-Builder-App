# Story 3.4: Proceed to Item Configuration

## Status: Done

## Story

**As a** user,
**I want** to confirm my subkit selections and proceed to configure items within each subkit,
**so that** I can complete my kit with the specific items I need.

## Acceptance Criteria

1. A clearly labeled Configure Items CTA button is displayed on the Subkit Selection screen
2. The CTA is disabled until the user has selected a minimum of 3 subkits
3. A friendly message is shown when fewer than 3 subkits are selected explaining the minimum requirement
4. Upon clicking the CTA, the app transitions to Item Configuration beginning with the first selected subkit
5. The order of subkit configuration follows the order in which they were selected
6. Subkit selections and size choices are preserved in application state throughout the flow

## Tasks / Subtasks

- [ ] Task 1: Create PrimaryButton component (AC: 1)
  - [ ] Create `src/components/ui/PrimaryButton.tsx`
  - [ ] Brand-primary color (`var(--color-brand-primary)`), hover state, focus-visible
  - [ ] Support `aria-disabled` pattern (not `disabled` attribute)
  - [ ] Props: `children`, `onClick`, `ariaDisabled?`, `ariaDescribedBy?`, `className?`
- [ ] Task 2: Add Configure Items CTA to SubkitSelectionScreen (AC: 1, 2, 3)
  - [ ] Render PrimaryButton with text "Configure Items"
  - [ ] Use `useCanProceedToConfig()` to determine enabled state
  - [ ] When `aria-disabled="true"`: show minimum message via `aria-describedby`
  - [ ] Message: "Choose at least 3 categories to continue"
  - [ ] CTA still focusable and reachable by keyboard when disabled
- [ ] Task 3: Implement navigation on click (AC: 4, 5)
  - [ ] On click (when enabled): navigate to `/configure/{firstSubkitId}`
  - [ ] First subkit determined by `selectionOrder: 1` from store
  - [ ] Set `currentConfigIndex` to 0 in store
  - [ ] Use `useNavigate()` from react-router-dom
- [ ] Task 4: Implement route guards (AC: 4, 6)
  - [ ] Update `src/router/guards.ts` with real guard logic:
    - `subkitConfigGuard`: redirect to `/` if `subkitId` not in selectedSubkits
    - `customConfigGuard`: redirect to `/` if 'custom' not in selectedSubkits
    - `summaryGuard`: redirect to `/` if fewer than 3 subkits
  - [ ] Guards use `useKitStore.getState()` (not hooks — loaders run outside React render)
- [ ] Task 5: Verify state preservation (AC: 6)
  - [ ] Navigate to item config and back — all selections and sizes preserved
  - [ ] Verify Zustand store maintains state across route changes
- [ ] Task 6: Screen transition animation (AC: 4)
  - [ ] Forward transition: exit `translateX(-16px)` + fade, enter from `+16px`
  - [ ] Duration: `var(--duration-screen)` (240ms)
  - [ ] Implement at router level via transition wrapper

## Dev Notes

### CTA Accessibility — Critical Rule
[Source: docs/architecture/11-frontend-developer-standards.md, Rule 6]
- The Configure Items CTA uses `aria-disabled` + `aria-describedby`, NOT the `disabled` attribute
- Keyboard users must be able to reach it and read the minimum message
- This is a hard architectural requirement

### Route Guards
[Source: docs/architecture/7-routing.md#route-configuration]
- Guards use `useKitStore.getState()` — not hooks — because loaders run outside React render cycle
- `subkitConfigGuard`: checks if subkitId param exists in selectedSubkits
- `customConfigGuard`: checks if any selectedSubkit has categoryId 'custom'
- `summaryGuard`: checks selectedSubkits.length >= 3

### Navigation Flow
[Source: docs/architecture/7-routing.md#navigation-flow]
```
/ → [Configure Items] → /configure/:firstSubkitId
```
- Config order follows selectionOrder from store
- `currentConfigIndex` tracks position in the sequence

### Screen Transitions
[Source: docs/architecture/7-routing.md#screen-transition-directions]
- Forward: exit translateX(-16px) + fade; enter from +16px
- Duration: var(--duration-screen) (240ms)
- Implemented at router level, not inside individual screen components

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test CTA disabled state with aria-disabled
- Test navigation triggers on click
- Test guard redirects
- axe assertions on CTA accessibility

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |

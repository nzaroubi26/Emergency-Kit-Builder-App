# Story 17.3: Subkit Ordering Utility

## Status: Review

## Story

**As a** developer,
**I want** a utility that returns subkit categories in the correct display order based on the user's MCQ emergency type,
**so that** the Fill Your Kit screen can render categories in priority order.

## Acceptance Criteria

1. A new file `src/utils/subkitOrdering.ts` exports:
   ```ts
   export function getOrderedCategories(
     emergencyType: EmergencyType | undefined,
     householdComposition: HouseholdOption[]
   ): string[]
   ```
2. The function implements the priority mapping:

   | Emergency Type | #1 | #2 | #3 |
   |----------------|----|----|-----|
   | Hurricane | Power | Medical | Communications |
   | Flood | Power | Clothing | Medical |
   | Tornado | Power | Cooking | Medical |
   | Tropical Storm | Power | Medical | Communications |

3. After the top 3, remaining categories follow default order: Power, Medical, Communications, Lighting, Cooking, Hygiene, Comfort, Clothing. Categories already placed in top 3 are skipped. Custom is excluded (no Amazon products).
4. Pets is appended last, only if `householdComposition` includes `'pets'`.
5. If `emergencyType` is undefined, the full default order is used.
6. Unit tests cover: each of the 4 emergency types produces correct ordering, undefined type uses default, Pets inclusion/exclusion based on household composition, no duplicate categories in output.

## Tasks / Subtasks

- [x] Task 1: Create subkitOrdering.ts (AC: 1, 2, 3, 4, 5)
  - [x] Create `src/utils/subkitOrdering.ts`
  - [x] Import `EmergencyType` and `HouseholdOption` from `src/store/mcqStore`
  - [x] Define `PRIORITY_MAP` with 4 emergency type entries
  - [x] Define `DEFAULT_ORDER` array (8 categories, no Custom, no Pets)
  - [x] Implement `getOrderedCategories` — top 3 from priority map, then remaining from default, then Pets if applicable
- [x] Task 2: Write unit tests (AC: 6)
  - [x] Create `tests/unit/subkitOrdering.test.ts`
  - [x] Test Hurricane ordering: Power, Medical, Communications, Lighting, Cooking, Hygiene, Comfort, Clothing
  - [x] Test Flood ordering: Power, Clothing, Medical, Communications, Lighting, Cooking, Hygiene, Comfort
  - [x] Test Tornado ordering: Power, Cooking, Medical, Communications, Lighting, Hygiene, Comfort, Clothing
  - [x] Test Tropical Storm ordering: Power, Medical, Communications, Lighting, Cooking, Hygiene, Comfort, Clothing
  - [x] Test undefined type: full default order
  - [x] Test Pets inclusion with `householdComposition: ['pets']`
  - [x] Test Pets exclusion with `householdComposition: []`
  - [x] Test no duplicates in any output
- [x] Task 3: Verify TypeScript and existing tests
  - [x] Run `tsc --noEmit` — zero errors
  - [x] Run `npm run test:run` — all tests pass

## Dev Notes

### Architecture Reference
[Source: docs/architecture-phase3-sprint3.md, Section 4]

Complete implementation is specified in the architecture brief, including the full function body. Key decisions:
1. **Separate from `elevationRules.ts`** — elevation (Sprint 2) determines visual emphasis in SubkitSelectionScreen. This ordering determines display sequence on Fill Your Kit. Different purpose, different consumers.
2. **`emergencyType` is singular** — caller passes `emergencyTypes[0]` from MCQ store. The utility doesn't access the store directly.
3. **Custom excluded** — not in `DEFAULT_ORDER` or `PRIORITY_MAP`.
4. **Pets always last** — appended after the ordered list, only if household includes pets.
5. **Pure function, no store dependency** — takes primitive inputs, returns string array. Fully testable.

### MCQ Mapping Rules
[Source: docs/sprint-3-mcq-mapping.md]

The priority table and default order come from the MCQ mapping document. The "first selected type wins" multi-select rule maps to `emergencyTypes[0]` in the MCQ store.

### Relevant Source Tree
```
src/
  utils/
    subkitOrdering.ts    # NEW — getOrderedCategories function
    elevationRules.ts    # UNCHANGED — different purpose (Sprint 2 visual emphasis)
  store/
    mcqStore.ts          # UNCHANGED — type imports only
tests/
  unit/
    subkitOrdering.test.ts  # NEW
```

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]
- Pure function, no side effects
- Named exports only
- TypeScript strict mode mandatory

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/utils/subkitOrdering.ts` | NEW | MCQ-driven category ordering with priority map and default fallback |
| `tests/unit/subkitOrdering.test.ts` | NEW | 8+ test cases covering all emergency types, defaults, Pets, no duplicates |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Completion Notes
- Created `src/utils/subkitOrdering.ts` with `getOrderedCategories` pure function
- `PRIORITY_MAP` covers all 4 emergency types with correct top-3 orderings
- `DEFAULT_ORDER` has 8 categories (no Custom, no Pets)
- Pets appended last only when `householdComposition` includes `'pets'`
- Undefined emergency type falls through to full default order
- 10 unit tests: all 4 emergency types, undefined fallback, Pets inclusion/exclusion, no duplicates, Custom exclusion
- `tsc --noEmit`: zero errors
- `npm run test:run`: 319/319 tests pass

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-14 | 1.0 | Initial story draft from PRD v1.3 Story 17.3 | Sally, UX Expert |

## QA Results

### Review Date: 2026-04-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment
Exemplary implementation — concise, pure, and correct. The entire utility is 28 lines including imports and whitespace. `PRIORITY_MAP` is typed as `Record<EmergencyType, [string, string, string]>` which enforces exactly 3 entries per emergency type at the type level. `DEFAULT_ORDER` is a plain array of 8 categories (Custom and Pets excluded by design). The algorithm is clean: spread top-3 from priority map (or empty array for undefined), filter default order to skip already-placed categories, append Pets conditionally. No store dependency — pure function with primitive inputs. Test suite is thorough with 10 tests covering all 5 code paths plus edge cases.

### Refactoring Performed
None required

### Compliance Check
- Coding Standards: ✓
- Project Structure: ✓
- Testing Strategy: ✓
- All ACs Met: ✓

### Acceptance Criteria Traceability
| AC | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| 1 | `subkitOrdering.ts` exports `getOrderedCategories(emergencyType, householdComposition): string[]` | ✓ MET | `src/utils/subkitOrdering.ts:15-17` — named export, correct signature with `EmergencyType \| undefined` and `HouseholdOption[]` params |
| 2 | Priority mapping: Hurricane [Power,Medical,Comms], Flood [Power,Clothing,Medical], Tornado [Power,Cooking,Medical], Tropical Storm [Power,Medical,Comms] | ✓ MET | `src/utils/subkitOrdering.ts:3-8` — `PRIORITY_MAP` entries match MCQ mapping doc Section 2 exactly. Manually traced all 4 paths: Hurricane→[power,medical,communications,lighting,cooking,hygiene,comfort,clothing], Flood→[power,clothing,medical,communications,lighting,cooking,hygiene,comfort], Tornado→[power,cooking,medical,communications,lighting,hygiene,comfort,clothing], Tropical Storm→same as Hurricane |
| 3 | Remaining categories follow default order, already-placed skipped, Custom excluded | ✓ MET | `src/utils/subkitOrdering.ts:10-13` DEFAULT_ORDER has 8 categories, no Custom. `:20` filter removes top-3 from remaining. Verified: Flood example from MCQ doc (positions 4-8: Communications, Lighting, Cooking, Hygiene, Comfort) matches implementation |
| 4 | Pets appended last only if household includes 'pets' | ✓ MET | `src/utils/subkitOrdering.ts:23-25` — `householdComposition.includes('pets')` guard with `ordered.push('pets')`. Tests confirm: length 9 with pets, length 8 without, Pets always at `result[result.length - 1]` |
| 5 | undefined emergencyType uses full default order | ✓ MET | `src/utils/subkitOrdering.ts:19` — ternary produces empty `top3` when undefined, so `rest` = full DEFAULT_ORDER. Test at line 37-43 of test file confirms exact sequence |
| 6 | Unit tests: all 4 types, undefined, Pets in/out, no duplicates | ✓ MET | `tests/unit/subkitOrdering.test.ts` — 10 tests: 4 emergency types with exact ordering assertions, undefined fallback, Pets inclusion (length 9, last position), Pets exclusion (length 8, two variants), no duplicates across all types, Custom never present. All pass as part of 319/319 |

### Improvements Checklist
No items — implementation is clean and complete.

### Security Review
No concerns. Pure utility function with no external I/O, no user input handling.

### Performance Considerations
No concerns. Operates on arrays of max 9 elements. Negligible overhead.

### Files Modified During Review
None

### Gate Status
Gate: PASS
Quality score: 98

### Recommended Status
✓ Ready for Done — all 6 ACs met with evidence, tsc clean, 319/319 tests pass (10 new), all orderings verified against sprint-3-mcq-mapping.md source of truth

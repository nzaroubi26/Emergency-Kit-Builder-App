# Story 2.1: Static Visualizer Component

## Status: Done

## Story

**As a** user,
**I want** to see a visual representation of my housing unit with 6 empty slots,
**so that** I can understand the physical structure of the storage system before making any selections.

## Dependencies

- Requires Story 1.2 (Data Architecture & Types) to be complete
- Requires Story 1.3 (Application Shell & Navigation) to be complete

## Acceptance Criteria

1. A self-contained Visualizer React component is created and rendered at the top of the Subkit Selection screen
2. The visualizer displays a single vertical column of 6 equal-sized rectangular slots, stacked top to bottom
3. Each empty slot displays a + icon centered within it
4. The visualizer is styled to resemble the interior of a rectangular storage container
5. The component accepts slot state as props — fully controlled and stateless internally
6. The visualizer renders correctly on desktop and tablet screen sizes
7. Component is documented with a clear props interface in TypeScript
8. When a slot is filled, it displays the subkit name and uses the category color as background
9. When readOnly is true, empty slots render as clean boxes without + icons and use 44px height

## Tasks / Subtasks

- [ ] Task 1: Create Zustand store and selector hooks (prerequisite for AC: 1)
  - [ ] Create `src/store/kitStore.ts` per architecture spec
  - [ ] Create `src/hooks/useKitStore.ts` with `useSlotState`, `useTotalSlotsUsed`, `useIsAtCapacity`, `useCanProceedToConfig`, `useItemSelection`, `useIsEmptyContainer`
- [ ] Task 2: Create HousingUnitVisualizer component (AC: 1, 2, 5, 7)
  - [ ] Create `src/components/visualizer/HousingUnitVisualizer.tsx`
  - [ ] Props: `slots: SlotState[]`, `readOnly?: boolean`, `onSlotClick?: (slotIndex: number) => void`
  - [ ] Render exactly 6 `VisualizerSlot` children based on `slots` array
  - [ ] Container styled as `max-w-sm mx-auto` centered per UX spec wireframe
  - [ ] Named export only, `FC<HousingUnitVisualizerProps>` typed
  - [ ] No internal state — fully props-driven
- [ ] Task 3: Create VisualizerSlot component (AC: 2, 3, 8, 9)
  - [ ] Create `src/components/visualizer/VisualizerSlot.tsx`
  - [ ] Accept `SlotState` + `slotIndex` + `readOnly` + `onSlotClick` as props
  - [ ] Empty state: display `Plus` icon from lucide-react, centered
  - [ ] Filled state: display subkit name, use `subkitColor` as background via inline style
  - [ ] readOnly + empty: no Plus icon, 44px height via inline style
  - [ ] Large blocks: isLargeStart/isLargeEnd visual merge (remove border between paired slots)
  - [ ] Add `data-testid="slot-{index}"` for testing
  - [ ] Each slot has equal height, stacked vertically with no gaps
- [ ] Task 4: Style visualizer container (AC: 4, 6)
  - [ ] Outer container resembles rectangular storage unit with border, rounded corners
  - [ ] Use `var(--radius-md)` for border radius
  - [ ] Use `var(--shadow-2)` for elevation
  - [ ] Slot borders use `var(--color-neutral-200)`
  - [ ] Responsive: `max-w-sm` centered, works on desktop and tablet
- [ ] Task 5: Render on Subkit Selection screen (AC: 1)
  - [ ] Import and render `HousingUnitVisualizer` at top of `SubkitSelectionScreen`
  - [ ] Pass slot state from `useSlotState()` hook (never call calculateSlotState directly)
  - [ ] Display "N of 6 slots used" text below visualizer using `useTotalSlotsUsed()`
- [ ] Task 6: Write component tests (AC: 1, 2, 3, 8, 9)
  - [ ] Create `tests/components/HousingUnitVisualizer.test.tsx`
  - [ ] Test: renders 6 slot containers
  - [ ] Test: shows subkit name when slot is filled
  - [ ] Test: hides + icons in readOnly mode
  - [ ] Test: uses 44px slot height in readOnly mode (check inline style)
  - [ ] Include axe accessibility assertion

## Dev Notes

### Component Architecture
[Source: docs/architecture/11-frontend-developer-standards.md, Rule 10]
- HousingUnitVisualizer receives NO store references
- Takes only `slots: SlotState[]`, `readOnly?`, and `onSlotClick?`
- All derivation happens in the parent component
- This is critical for NFR6 — self-contained extensible module

### Props Interface
[Source: docs/architecture/5-state-management.md#type-definitions]
Already defined in `src/types/visualizer.types.ts` (created in Story 1.2):
```typescript
interface HousingUnitVisualizerProps {
  slots: SlotState[];              // Always length 6; index 0 = top slot
  readOnly?: boolean;              // true on Summary Page
  onSlotClick?: (slotIndex: number) => void;  // Phase 2 — wired, dormant in MVP
}
```

### SlotState Type
Already defined in `src/types/visualizer.types.ts` (created in Story 1.2):
```typescript
interface SlotState {
  status: 'empty' | 'filled';
  subkitId?: string;
  subkitName?: string;
  subkitColor?: string;    // Hex from category colorBase
  size: SubkitSize;
  isLargeStart?: boolean;
  isLargeEnd?: boolean;
}
```

### Store and Hooks (prerequisite)
[Source: docs/architecture/5-state-management.md]
The Zustand store and selector hooks are prerequisites for this story. Architecture Rule #2: "Never call calculateSlotState() directly in components — always use useSlotState()."
- Store: `src/store/kitStore.ts` — full implementation in architecture spec
- Hooks: `src/hooks/useKitStore.ts` — useSlotState, useTotalSlotsUsed, useIsAtCapacity, etc.

### Slot Fill Direction
[Source: docs/prd/6-epic-details.md, UX Spec Decision #2]
- Top-to-bottom fill order (NOT bottom-up — this is a brownfield correction)

### Visualizer Placement
[Source: UX Spec Decision #9]
- Stacked above cards, centered, max-w-sm

### Animation (context for later stories — NOT in scope)
[Source: docs/architecture/8-styling-guidelines.md#animation-rules]
- Slot fill: `background-color` transition over 220ms
- Name label: `opacity` transition delayed 80ms
- GPU-composited properties only (transform, opacity)

### Testing

[Source: docs/architecture/9-testing-requirements.md#component-test-template]
- Full test template provided in architecture
- Tests: renders 6 slots, shows subkit name when filled, hides + in readOnly, 44px height in readOnly
- Must include axe accessibility assertion (`axe(container)` + `toHaveNoViolations()`)
- NOTE: lucide-react icons render as SVGs, not img elements. Use appropriate query method.
- Install `axe-core` and `jest-axe` if not already present

## PO Validation Results

| Category | Status | Issues |
|----------|--------|--------|
| 1. Goal & Context Clarity | PASS | Added dependencies on Stories 1.2 and 1.3 |
| 2. Technical Implementation Guidance | PASS | Added store/hooks prerequisite task |
| 3. Reference Effectiveness | PASS | Types already created in Story 1.2 noted |
| 4. Self-Containment Assessment | PASS | Store/hooks gap resolved with Task 1 |
| 5. Testing Guidance | PASS | Noted lucide-react SVG rendering caveat |

**Validation Date:** 2026-03-03
**Validated By:** PO Agent
**Assessment:** READY FOR IMPLEMENTATION

### Fixes Applied During Validation
1. Added Dependencies section
2. Added Task 1 (store + hooks creation) — prerequisite not covered by any prior story
3. Added AC 8 (filled slot display with name/color) and AC 9 (readOnly behavior)
4. Noted lucide-react SVG rendering caveat for tests (not img role)
5. Clarified animations are NOT in scope for this story

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |
| 2026-03-03 | 1.1 | PO validation: added dependencies, store task, AC 8/9, test caveats | PO Agent |

## Dev Agent Record

### Agent Model Used
_To be filled during implementation_

### Debug Log References
_To be filled during implementation_

### Completion Notes List
_To be filled during implementation_

### File List
_To be filled during implementation_

## QA Results
_To be filled after implementation_

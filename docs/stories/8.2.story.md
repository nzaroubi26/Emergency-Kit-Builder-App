# Story 8.2: E-commerce Checkout Integration

## Status: Done

## Story

**As a** user,
**I want** clicking "Get My Kit" to initiate a real checkout for my configured kit,
**so that** I can purchase my emergency preparedness kit directly from the builder.

## Acceptance Criteria

1. A `src/services/checkoutService.ts` module is created, exporting an async `initiateCheckout(selectedSubkits: SubkitSelection[], itemSelections: Record<string, ItemSelection>, emptyContainers: string[]): Promise<CheckoutResult>` function. The function builds a typed `CheckoutPayload` internally before dispatching the API call — `SummaryScreen` passes store state directly and is not coupled to the payload shape.
2. `CheckoutPayload` is a typed internal interface containing the serialized kit data including a `kitId` (UUID generated at call time for idempotency), subkit selections, sizes, item quantities, and empty container flags.
3. `initiateCheckout` calls `ENV.purchaseUrl` via a POST `fetch` request with the serialized payload.
4. On a successful API response, the user is redirected to the checkout URL returned in the response in the **same window** (`window.location.href = data.redirectUrl`). A new tab is not used — same-window is the correct e-commerce pattern, and localStorage persistence (Story 6.1) ensures the kit configuration is fully restored if the user navigates back.
5. On API failure (network error or non-2xx response), a user-facing error message is displayed on the Summary Page below the CTA. The error is dismissible. Kit state is fully preserved — no data is lost on failure.
6. During the API call, the "Get My Kit" CTA displays a loading state (disabled with "Processing..." label) to prevent duplicate submissions.
7. The `cta_clicked` analytics event fires before the API call is initiated — not contingent on API success.
8. `checkoutService.ts` is covered by unit tests mocking `fetch` for: successful response with redirect URL, network failure, and non-2xx response.

## Tasks / Subtasks

- [x] Task 1: Create checkoutService module (AC: 1, 2, 3)
  - [x] Define CheckoutPayload typed interface with kitId (UUID), subkit selections, sizes, item quantities, empty container flags
  - [x] Define CheckoutResult type with success/failure discriminated union
  - [x] Implement buildPayload helper serializing store state into CheckoutPayload
  - [x] Implement initiateCheckout calling ENV.purchaseUrl via POST fetch
- [x] Task 2: Implement success redirect (AC: 4)
  - [x] On successful API response, redirect via window.location.href = data.redirectUrl
  - [x] Same-window redirect (no new tab)
  - [x] localStorage persistence ensures kit restoration on back-navigation
- [x] Task 3: Implement error handling on Summary Page (AC: 5)
  - [x] Add checkoutError local state to SummaryScreen
  - [x] Display dismissible error message below CTA on API failure
  - [x] Kit state fully preserved on failure — no data loss
- [x] Task 4: Implement loading state (AC: 6)
  - [x] Add checkoutLoading local state to SummaryScreen
  - [x] CTA shows "Processing..." and is disabled during API call
  - [x] Prevents duplicate submissions
- [x] Task 5: Analytics integration (AC: 7)
  - [x] Fire cta_clicked analytics event before API call
  - [x] Event fires regardless of API outcome
- [x] Task 6: Unit tests for checkoutService (AC: 8)
  - [x] Test successful response with redirect URL
  - [x] Test network failure handling
  - [x] Test non-2xx response handling
  - [x] Test payload construction with correct shape

## Dev Notes

### Checkout Service Architecture
[Source: docs/prd-phase2.md Section 4]
- New `src/services/checkoutService.ts` serializes `selectedSubkits` and `itemSelections` into a cart payload and calls `ENV.purchaseUrl` via `fetch`
- Checkout errors surface via local component state on `SummaryScreen` — kit state is never lost on failure

### ENV Configuration
[Source: docs/architecture/10-environment-configuration.md]
- `ENV.purchaseUrl` defined in `src/tokens/env.ts`
- Uses `VITE_` prefix for Vite environment variable access

### Analytics Event
[Source: docs/prd-phase2.md Section 2]
- `cta_clicked` event fires on "Get My Kit" click, before API call

### Testing
[Source: docs/architecture/9-testing-requirements.md]
- Unit tests mock `fetch` for success, network failure, and non-2xx responses
- Test payload construction including UUID kitId, subkit data, item quantities, empty container flags

## Dev Agent Record

### Files Changed
- `src/services/checkoutService.ts` — CheckoutPayload, CheckoutResult types, buildPayload, initiateCheckout
- `src/components/summary/SummaryScreen.tsx` — checkoutLoading, checkoutError states, handleGetMyKit, error alert, Processing... button
- `src/tokens/env.ts` — ENV.purchaseUrl
- `tests/unit/checkoutService.test.ts` — 6 tests: payload, empty containers, success, errors

### QA Results
Retroactively documented — implementation verified by PO assessment 2026-03-05

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-05 | 1.0 | Retroactive story creation — implementation complete | SM Agent |

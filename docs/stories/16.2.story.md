# Story 16.2: Build My Own → `/review` Wiring + E2E

## Status: Ready for Review

## Story

**As a** user who built my own kit,
**I want** to proceed from the summary page to Review & Order and then place my order,
**so that** I complete the same order flow as users who chose the Essentials path.

## Acceptance Criteria

1. SummaryScreen's "Get My Kit" CTA is updated: calls `useMCQStore.getState().setKitPath('custom')`, then navigates to `/review`.
2. `reviewGuard` passes for `kitPath === 'custom'` with no guard changes (Sprint 1 designed for this — truthy check on `!kitPath`).
3. Review & Order page displays the custom `KitSummaryCard` with the user's kit. Delivery section and "Place Order" CTA function identically to the Essentials path.
4. Back navigation from `/review` is path-aware. Replace the hardcoded `<BackLink to="/choose" label="Back" />` (line 24 of `ReviewOrderScreen.tsx`) with:
   - `kitPath === 'essentials'` → `to="/choose"` label `"Back"`
   - `kitPath === 'custom'` → `to="/summary"` label `"Back to Kit Summary"`
5. "Place Order" navigates to `/confirmation`.
6. Smoke E2E test covers the full Build My Own happy path: cover → MCQ → fork → `/builder` → select subkits → configure items → `/summary` → `/review` → `/confirmation`.

## Tasks / Subtasks

- [x] Task 1: Update SummaryScreen "Get My Kit" CTA (AC: 1)
  - [x] Import `useMCQStore` in `SummaryScreen.tsx`
  - [x] In the "Get My Kit" CTA handler: call `useMCQStore.getState().setKitPath('custom')`
  - [x] Navigate to `/review` instead of `/confirmation`
- [x] Task 2: Update ReviewOrderScreen back navigation (AC: 4)
  - [x] Replace hardcoded `<BackLink to="/choose" label="Back" />` with path-aware version
  - [x] Read `kitPath` from `useMCQStore` (already in scope at line 12)
  - [x] `to={kitPath === 'custom' ? '/summary' : '/choose'}`
  - [x] `label={kitPath === 'custom' ? 'Back to Kit Summary' : 'Back'}`
- [x] Task 3: Write E2E test (AC: 6)
  - [x] Create smoke E2E test for Build My Own happy path
  - [x] Cover: cover → MCQ-1 → MCQ-2 → fork → `/builder` → select subkits → configure items → `/summary` → `/review` → `/confirmation`
  - [x] Verify kit summary on `/review` matches selections ("Your Custom Kit", "Back to Kit Summary")
  - [x] Verify "Place Order" navigates to `/confirmation`
  - [x] Also fixed Sprint 1 E2E: heading "Build Your Kit" → "Build Your Emergency Kit" (15.2 change)
- [x] Task 4: Verify no regressions (AC: 2, 3, 5)
  - [x] `reviewGuard` passes for `kitPath === 'custom'` with no changes (zero guard code modified)
  - [x] ReviewOrderScreen tests: 7/7 pass
  - [x] Run `tsc --noEmit` — zero errors

## Dev Notes

### Architecture Reference
[Source: docs/architecture-phase3-sprint2.md, Sections 5 and 6]

**Back navigation — inline derivation:** The `BackLink` component already accepts `to: string` and `label?: string`. The `ReviewOrderScreen` already reads `kitPath` from `useMCQStore` (line 12). Two-line change, no abstraction needed.

**Guard compatibility:** `reviewGuard` checks `!kitPath` — fails for `null`, passes for `'essentials'` and `'custom'`. Zero guard changes needed.

### Current Code
[Source: src/components/summary/SummaryScreen.tsx]
Current "Get My Kit" CTA (line 122–128):
```tsx
<PrimaryButton
  onClick={() => {
    Analytics.ctaClicked();
    navigate('/confirmation');
  }}
  className="btn-get-my-kit"
>
  Get My Kit
</PrimaryButton>
```
Sprint 2 change: add `useMCQStore.getState().setKitPath('custom')` before `navigate('/review')`.

[Source: src/components/review/ReviewOrderScreen.tsx]
Current back link (line 24):
```tsx
<BackLink to="/choose" label="Back" />
```
Sprint 2 change: derive `to` and `label` from `kitPath`.

### Relevant Source Tree
```
src/
  components/
    summary/
      SummaryScreen.tsx          # MODIFY — CTA re-route
    review/
      ReviewOrderScreen.tsx      # MODIFY — path-aware back link
tests/
  e2e/
    build-my-own-flow.spec.ts    # NEW — E2E smoke test
```

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]
- All 14 critical rules apply
- Named exports only

### Testing
[Source: docs/architecture/9-testing-requirements.md]
- New E2E test: Build My Own happy path
- Essentials path E2E must still pass (regression check)
- Run `tsc --noEmit` and `npm run test:run`

### Integration Verification
- IV1: Full Build My Own path navigable end-to-end.
- IV2: Kit summary on `/review` matches the summary on `/summary`.
- IV3: "Place Order" navigates to `/confirmation` with kit data intact.
- IV4: Back navigation from `/review` returns to `/summary`.
- IV5: Essentials path E2E still passes (no regression).

## File List

| File | Action | Description |
|------|--------|-------------|
| src/components/summary/SummaryScreen.tsx | MODIFY | "Get My Kit" CTA → `setKitPath('custom')` + navigate to `/review` |
| src/components/review/ReviewOrderScreen.tsx | MODIFY | Path-aware `BackLink` derived from `kitPath` |
| tests/e2e/build-my-own-flow.spec.ts | NEW | Smoke E2E test for Build My Own happy path |

## Dev Agent Record

### Agent Model Used
claude-opus-4-6

### Debug Log References
None — clean implementation.

### Completion Notes
- SummaryScreen "Get My Kit" CTA: `setKitPath('custom')` + `navigate('/review')` (was `/confirmation`)
- ReviewOrderScreen back link: path-aware — custom → `/summary` "Back to Kit Summary", essentials → `/choose` "Back"
- E2E smoke test covers full Build My Own happy path through all screens
- Fixed Sprint 1 E2E heading assertion for 15.2 change
- Zero guard changes needed — `reviewGuard` already passes for `kitPath === 'custom'`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-14 | 1.0 | Initial story draft | Sarah, PO Agent |
| 2026-04-14 | 1.1 | Implementation complete — wiring + E2E | James, Dev Agent |

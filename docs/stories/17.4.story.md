# Story 17.4: Add to Cart URL Utility

## Status: Review

## Story

**As a** developer,
**I want** a utility that constructs an Amazon Add to Cart URL from an array of ASINs,
**so that** the Fill Your Kit screen can offer a one-click "Add All to Amazon Cart" CTA.

## Acceptance Criteria

1. A new file `src/utils/cartUrl.ts` exports:
   ```ts
   export function buildCartUrl(asins: string[], tag?: string): string
   ```
2. The function constructs a URL using `URLSearchParams`:
   ```
   https://www.amazon.com/gp/aws/cart/add.html
     ?AssociateTag={TAG}&tag={TAG}
     &ASIN.1={ASIN_1}&Quantity.1=1
     &ASIN.2={ASIN_2}&Quantity.2=1
     ...
   ```
   Both `AssociateTag` and `tag` parameters are included for compatibility (per spike findings).
3. ASINs are numbered sequentially starting at 1. Quantity is always 1.
4. The affiliate tag defaults to the same `AFFILIATE_TAG` constant from `affiliateLink.ts`.
5. Empty array returns empty string. Single ASIN works correctly.
6. Unit tests cover: correct URL format with 1 ASIN, correct URL with multiple ASINs, sequential numbering, custom tag override, empty array returns empty string, URL length is reasonable for 31 ASINs (under 2,000 characters).

## Tasks / Subtasks

- [x] Task 1: Create cartUrl.ts (AC: 1, 2, 3, 4, 5)
  - [x] Create `src/utils/cartUrl.ts`
  - [x] Import `AFFILIATE_TAG` from `./affiliateLink`
  - [x] Define `CART_BASE` constant
  - [x] Implement `buildCartUrl` using `URLSearchParams`
  - [x] Handle empty array edge case (return `''`)
- [x] Task 2: Write unit tests (AC: 6)
  - [x] Create `tests/unit/cartUrl.test.ts`
  - [x] Test single ASIN URL format
  - [x] Test multiple ASIN URL with sequential numbering
  - [x] Test custom tag override
  - [x] Test empty array returns empty string
  - [x] Test URL length with all 31 catalog ASINs (under 2,000 chars)
  - [x] Verify both `AssociateTag` and `tag` params present
- [x] Task 3: Verify TypeScript and existing tests
  - [x] Run `tsc --noEmit` — zero errors
  - [x] Run `npm run test:run` — all tests pass

## Dev Notes

### Depends On
- **Story 17.2** must be completed first — this story imports `AFFILIATE_TAG` from `affiliateLink.ts`.

### Architecture Reference
[Source: docs/architecture-phase3-sprint3.md, Section 3]

Complete implementation is specified in the architecture brief, including the full function body. Key decisions:
1. **`URLSearchParams` instead of manual string concatenation** — handles encoding correctly
2. **Both `AssociateTag` and `tag` included** — for compatibility across Amazon endpoint variants
3. **Quantity is always 1** — per PRD FR22
4. **URL length verified** — 31 ASINs produce ~1,400 characters, well under 2,000-char browser limit

### Spike Finding: 90-Day Cookie
[Source: docs/sprint-3-api-spike-results.md]

Items placed in cart via the Add to Cart URL earn affiliate commission if purchased within **90 days** — 90x longer than the standard 24-hour affiliate link cookie. This makes the cart URL the superior primary affiliate mechanism.

### Relevant Source Tree
```
src/
  utils/
    cartUrl.ts           # NEW — buildCartUrl function
    affiliateLink.ts     # UNCHANGED — AFFILIATE_TAG imported
tests/
  unit/
    cartUrl.test.ts      # NEW
```

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]
- Pure function, no side effects
- Named exports only
- TypeScript strict mode mandatory

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/utils/cartUrl.ts` | NEW | `buildCartUrl` function using URLSearchParams |
| `tests/unit/cartUrl.test.ts` | NEW | 6+ test cases covering format, numbering, edge cases |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Completion Notes
- Created `src/utils/cartUrl.ts` with `buildCartUrl` pure function
- Uses `URLSearchParams` for correct encoding; includes both `AssociateTag` and `tag` params
- Imports `AFFILIATE_TAG` from `affiliateLink.ts` (single source of truth)
- Empty array returns empty string; ASINs numbered sequentially starting at 1; Quantity always 1
- 6 unit tests: single ASIN, multiple ASINs, custom tag, empty array, both tag params present, URL length under 2000 chars for all 31 products
- `tsc --noEmit`: zero errors
- `npm run test:run`: 325/325 tests pass

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-14 | 1.0 | Initial story draft from PRD v1.3 Story 17.4 | Sally, UX Expert |

## QA Results

_To be filled by QA agent_

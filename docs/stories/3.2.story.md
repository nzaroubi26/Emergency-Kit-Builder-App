# Story 3.2: Regular/Large Size Selection Per Subkit

## Status: Done

## Story

**As a** user,
**I want** to choose whether each selected subkit is Regular or Large sized,
**so that** I can allocate more space to the subkits that matter most to my household.

## Acceptance Criteria

1. Upon selecting a subkit card, the user is presented with a size option — Regular or Large
2. Regular and Large options clearly communicate their slot usage (1 slot vs 2 slots)
3. Size selection is displayed inline on the subkit card — no full-page navigation required
4. Selecting Large when insufficient slots remain shows a clear, friendly error message
5. The user can change a subkit size at any time — the visualizer updates immediately
6. Default size is Regular unless the user explicitly chooses Large

## Tasks / Subtasks

- [ ] Task 1: Create SizeToggle component (AC: 1, 2, 3)
  - [ ] Create `src/components/subkit-selection/SizeToggle.tsx`
  - [ ] Display `[Regular]` `[Large]` toggle with slot count labels ("1 slot" / "2 slots")
  - [ ] Inline on the subkit card — slides in on selection (animation)
  - [ ] Active option highlighted with category color
  - [ ] Inactive option neutral styling
  - [ ] `role="radiogroup"` with `role="radio"` per option, `aria-checked`
- [ ] Task 2: Wire SizeToggle to SubkitCard (AC: 1, 3, 6)
  - [ ] Show SizeToggle only when card is selected
  - [ ] Slide-in animation: `var(--duration-default)` (150ms) with `var(--ease-decelerate)`
  - [ ] Default selection: Regular
  - [ ] On toggle: call `setSubkitSize(subkitId, size)` from store
- [ ] Task 3: Handle blocked size changes (AC: 4)
  - [ ] When `setSubkitSize` returns `false`, display inline amber message
  - [ ] Message: "Not enough slots for Large size. Remove or resize another subkit."
  - [ ] Use `var(--color-status-warning)` color
  - [ ] No modal — inline message only
  - [ ] ARIA: `announceAssertive()` with blocked message
  - [ ] Message auto-dismisses after 3 seconds or on next interaction
- [ ] Task 4: Visualizer sync on size change (AC: 5)
  - [ ] Verify visualizer updates immediately on Regular ↔ Large toggle
  - [ ] Large → Regular: slot freed, visualizer updates, disabled cards may re-enable
  - [ ] Regular → Large: extra slot consumed, visualizer updates
- [ ] Task 5: Write tests (AC: 1, 4, 6)
  - [ ] Test: SizeToggle renders Regular and Large options
  - [ ] Test: default selection is Regular
  - [ ] Test: toggling calls store action
  - [ ] Test: blocked message appears when insufficient slots
  - [ ] Include axe accessibility assertion

## Dev Notes

### SizeToggle Location
[Source: docs/architecture/3-project-structure.md]
- File: `src/components/subkit-selection/SizeToggle.tsx`
- `[Regular][Large]` inline toggle; slides in on selection

### Store Action
[Source: docs/architecture/5-state-management.md#store]
- `setSubkitSize(subkitId, size)` returns `boolean` — false means not enough slots
- Internally calls `canFitSize()` to check feasibility before applying

### Animation
[Source: docs/architecture/8-styling-guidelines.md]
- Slide-in uses transform + opacity only (GPU-composited)
- Respect `prefers-reduced-motion`

### UX Spec Decisions
[Source: UX Spec Decision #1]
- Regular/Large size toggle kept per PRD — user chooses size, visualizer reflects it

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test behavior (getByRole radiogroup) not implementation
- axe assertion required

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |

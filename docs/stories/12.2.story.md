# Story 12.2: Container Price Display on EmptyContainerOption

## Status: Done

## Story

**As a** kit builder user,
**I want** to see the price of an empty container displayed next to the checkbox when I choose the empty container option,
**so that** I know exactly how much the empty container costs based on my selected subkit size before committing.

## Acceptance Criteria

1. **AC1 — Container price displayed inline with checkbox label.**
   In `src/components/item-config/EmptyContainerOption.tsx`, a price element renders below the existing label text "Send an empty container". The price reads `$40.00` for Regular size or `$60.00` for Large size, computed as `'$' + CONTAINER_PRICES[containerSize].toFixed(2)`.

2. **AC2 — Price sourced from `CONTAINER_PRICES` constant.**
   The price value is sourced from the existing `CONTAINER_PRICES` constant imported from `src/utils/cartCalculations.ts`. No hardcoded price values in the component.

3. **AC3 — Price typography and color match spec.**
   The price element uses `text-xs` (12px) with `font-normal` (400 weight) and color `text-[var(--color-neutral-500)]` — matching `text-caption` / `neutral-500` from the front-end spec.

4. **AC4 — New `containerSize` prop added to `EmptyContainerOptionProps`.**
   A new required prop `containerSize: 'regular' | 'large'` is added to `EmptyContainerOptionProps`. This is the only interface change — it is additive and does not break existing props.

5. **AC5 — Parent components pass `containerSize` prop.**
   - `ItemConfigScreen.tsx`: passes `containerSize={currentSubkit?.size ?? 'regular'}` (the `currentSubkit` variable already exists at line 151).
   - `CustomSubkitScreen.tsx`: passes `containerSize={customSubkit?.size ?? 'regular'}` (the `customSubkit` variable already exists at line 153).

6. **AC6 — Price updates reactively if subkit size changes.**
   The displayed price reflects the current subkit size. If the user changes size on a prior screen and returns, the price updates accordingly — this is automatic because the parent reads from the store.

7. **AC7 — Existing EmptyContainerOption tests updated and new tests added.**

   All existing tests in `tests/components/EmptyContainerOption.test.tsx` are updated to pass the new `containerSize` prop. New tests added:

   | Test | Assertion |
   |---|---|
   | Renders $40.00 for regular size | `screen.getByText('$40.00')` present when `containerSize="regular"` |
   | Renders $60.00 for large size | `screen.getByText('$60.00')` present when `containerSize="large"` |
   | axe accessibility passes with price displayed | `expect(results).toHaveNoViolations()` (update existing axe tests to include `containerSize`) |

8. **AC8 — TypeScript strict compilation and all tests pass.**
   `npm run typecheck` and `npm run test:run` both pass with zero errors.

## Tasks / Subtasks

- [x] Task 1: Add `containerSize` prop and price display to EmptyContainerOption (AC: 1, 2, 3, 4)
  - [x] Open `src/components/item-config/EmptyContainerOption.tsx`
  - [x] Add import: `import { CONTAINER_PRICES } from '../../utils/cartCalculations';`
  - [x] Add `import type { SubkitSize } from '../../types';`
  - [x] Add `containerSize: SubkitSize;` to the `EmptyContainerOptionProps` interface
  - [x] Add `containerSize` to the destructured props
  - [x] Compute the formatted price above the return: `const formattedPrice = '$' + CONTAINER_PRICES[containerSize].toFixed(2);`
  - [x] Inside the `<label>` element, below the existing `<span>` with "Send an empty container", add:
    ```tsx
    <span className="block text-xs font-normal text-[var(--color-neutral-500)]">
      {formattedPrice}
    </span>
    ```
  - [x] Verify no other changes to the component logic

- [x] Task 2: Pass `containerSize` prop from `ItemConfigScreen` (AC: 5)
  - [x] Open `src/components/item-config/ItemConfigScreen.tsx`
  - [x] Locate the `<EmptyContainerOption>` usage (around line 210)
  - [x] Add prop: `containerSize={currentSubkit?.size ?? 'regular'}`
  - [x] Verify `currentSubkit` is already defined at line 151: `const currentSubkit = sorted.find((s) => s.subkitId === subkitId);`

- [x] Task 3: Pass `containerSize` prop from `CustomSubkitScreen` (AC: 5)
  - [x] Open `src/components/item-config/CustomSubkitScreen.tsx`
  - [x] Locate the `<EmptyContainerOption>` usage (around line 239)
  - [x] Add prop: `containerSize={customSubkit?.size ?? 'regular'}`
  - [x] Verify `customSubkit` is already defined at line 153: `const customSubkit = sorted.find((s) => s.subkitId === SUBKIT_ID);`

- [x] Task 4: Update existing tests and add new tests (AC: 7)
  - [x] Open `tests/components/EmptyContainerOption.test.tsx`
  - [x] Update ALL existing `render(<EmptyContainerOption .../>)` calls to include `containerSize="regular"` — there are 8 existing tests, each with a render call
  - [x] Add Test 1: `'renders $40.00 for regular size'` — render with `containerSize="regular"`, assert `screen.getByText('$40.00')` present
  - [x] Add Test 2: `'renders $60.00 for large size'` — render with `containerSize="large"`, assert `screen.getByText('$60.00')` present
  - [x] Verify existing axe tests still pass (they will, since `containerSize` is now included)
  - [x] Verify all 8 existing tests pass with the new prop added

- [x] Task 5: Run full verification suite (AC: 8)
  - [x] Run `npm run typecheck` — zero errors
  - [x] Run `npm run test:run` — all new and existing tests green (267/267 passed)
  - [x] Run `npm run lint` — no new lint errors (pre-existing empty interface warnings only)

## Dev Notes

### Epic Context
[Source: docs/prd-phase2.5/epic-12-pricing-display.md]

Story 12.2 is the second story in Epic 12 (Inline Pricing Display). It depends on Story 12.1 being complete (for sequencing, though technically independent). This story adds the container price display to the `EmptyContainerOption` checkbox on both `ItemConfigScreen` and `CustomSubkitScreen`.

### Previous Story Insights
[Source: docs/stories/12.1.story.md]

**Story 12.1** (Item Price on ItemCard) adds per-item pricing to `ItemCard`. No carry-over issues expected — the two features modify different components.

### Current EmptyContainerOption Component
[Source: src/components/item-config/EmptyContainerOption.tsx]

```typescript
interface EmptyContainerOptionProps {
  checked: boolean;
  categoryColor: string;
  onChange: () => void;
}
```

The component renders:
- A `<div>` container with border
- A `<label>` wrapping a `<input type="checkbox">` and a `<span>` with text "Send an empty container"
- A conditional confirmation `<p>` when checked: "This subkit will be shipped as an empty container."

The price element should be added as a `<span>` below the existing label text, inside the `<label>` element.

### CONTAINER_PRICES Constant
[Source: src/utils/cartCalculations.ts]

```typescript
export const CONTAINER_PRICES: Record<'regular' | 'large', number> = {
  regular: 40,
  large: 60,
};
```

### Parent Component Wiring
[Source: src/components/item-config/ItemConfigScreen.tsx, src/components/item-config/CustomSubkitScreen.tsx]

**ItemConfigScreen** (line ~151, ~210):
```typescript
const currentSubkit = sorted.find((s) => s.subkitId === subkitId);
// ...
<EmptyContainerOption
  checked={isEmpty}
  categoryColor={category.colorBase}
  onChange={() => toggleEmptyContainer(subkitId)}
/>
```
Add: `containerSize={currentSubkit?.size ?? 'regular'}`

**CustomSubkitScreen** (line ~153, ~239):
```typescript
const customSubkit = sorted.find((s) => s.subkitId === SUBKIT_ID);
// ...
<EmptyContainerOption
  checked={isEmpty}
  categoryColor={category.colorBase}
  onChange={() => toggleEmptyContainer(SUBKIT_ID)}
/>
```
Add: `containerSize={customSubkit?.size ?? 'regular'}`

### SubkitSize Type
[Source: src/types/kit.types.ts]

```typescript
export type SubkitSize = 'regular' | 'large';
```

### Front-End Spec — Phase 2.7 Container Price
[Source: docs/front-end-spec.md#EmptyContainerOption Component]

| Element | Spec |
|---------|------|
| Price text | `$40.00` (Regular) or `$60.00` (Large) — `CONTAINER_PRICES[subkit.size].toFixed(2)` prefixed with `$` |
| Typography | `text-caption` (12px / 400) — maps to Tailwind `text-xs font-normal` |
| Color | `neutral-500` (`#6B7280`) — `text-[var(--color-neutral-500)]` |
| Placement | Below or alongside the existing checkbox label text |
| Reactivity | Updates immediately if the subkit size is changed |

### Existing Tests
[Source: tests/components/EmptyContainerOption.test.tsx]

8 existing tests, all rendering `EmptyContainerOption` without a `containerSize` prop. All must be updated to include `containerSize="regular"` to satisfy the new required prop. No other test logic changes needed for existing tests.

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]

- Named exports only — no `export default`
- `FC<Props>` typed components with co-located `{ComponentName}Props` interface
- No logic in JSX — extract to `const` above return
- TypeScript strict mode must pass (`tsc --noEmit`)

### Compatibility Constraints
[Source: docs/prd-phase2.5/epic-12-pricing-display.md#Compatibility Requirements]

- **CC1:** Zustand store shape is unchanged. No new fields.
- **CC2:** `cartCalculations.ts` is not modified — only importing `CONTAINER_PRICES` (read-only).
- **CC3:** `EmptyContainerOptionProps` is extended additively — new `containerSize` prop added, no breaking changes to existing props.
- **CC4–CC6:** All Phase 2.5 and Phase 2.6 features are unaffected.

### Testing

[Source: docs/architecture/9-testing-requirements.md, tests/components/EmptyContainerOption.test.tsx]

- **Modified test file:** `tests/components/EmptyContainerOption.test.tsx`
- **Framework:** Vitest + React Testing Library + vitest-axe
- **axe pattern:**
  ```typescript
  import { axe } from 'vitest-axe';
  import * as matchers from 'vitest-axe/matchers';
  expect.extend(matchers);
  ```
- 8 existing tests updated with `containerSize` prop + 2 new price-specific tests.

## Dev Agent Record

**Completed:** 2026-03-11
**Status:** All acceptance criteria met, all verification checks passed.

### Completion Notes
- Added `containerSize: SubkitSize` prop to `EmptyContainerOptionProps` interface
- Price display rendered as `<span>` below label text using `CONTAINER_PRICES[containerSize].toFixed(2)`
- Price typography: `text-xs font-normal text-[var(--color-neutral-500)]` per spec
- Both parent components (`ItemConfigScreen`, `CustomSubkitScreen`) pass `containerSize` from their existing subkit lookup
- All 8 existing tests updated with `containerSize="regular"` prop
- 2 new tests added: regular ($40.00) and large ($60.00) price rendering
- No modifications to `cartCalculations.ts` — import only
- No new Zustand store fields

### Files Modified
| File | Change |
|------|--------|
| `src/components/item-config/EmptyContainerOption.tsx` | Added `containerSize` prop, `CONTAINER_PRICES` import, `formattedPrice` computation, price `<span>` in label |
| `src/components/item-config/ItemConfigScreen.tsx` | Added `containerSize={currentSubkit?.size ?? 'regular'}` to `<EmptyContainerOption>` |
| `src/components/item-config/CustomSubkitScreen.tsx` | Added `containerSize={customSubkit?.size ?? 'regular'}` to `<EmptyContainerOption>` |
| `tests/components/EmptyContainerOption.test.tsx` | Updated 8 existing render calls with `containerSize="regular"`, added 2 new price tests |

### Verification Results
- `npm run typecheck` — zero errors
- `npm run test:run` — 267/267 tests passed (22 test files)
- `npm run lint` — no new lint errors

## QA Results

### Review Date: 2026-03-11

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is clean, focused, and well-structured. The change is purely additive — a single new prop flows from two parent components into `EmptyContainerOption`, which renders the price sourced from the existing `CONTAINER_PRICES` constant. No store changes, no new modules, no complex logic. The component follows project conventions: named export, `FC<Props>` typing, co-located props interface, and computation extracted above the return statement.

### Refactoring Performed

None required. The implementation is minimal and correct as-is.

### Compliance Check

- Coding Standards: ✓ Named exports, FC typing, no logic in JSX, extracted `formattedPrice` const
- Project Structure: ✓ No new files; changes limited to existing component and test files
- Testing Strategy: ✓ All existing tests updated with new prop; 2 new price-specific tests; axe accessibility tests pass
- All ACs Met: ✓ All 8 acceptance criteria verified (see detailed trace below)

### AC Trace

| AC | Status | Evidence |
|----|--------|----------|
| AC1 | ✓ | `EmptyContainerOption.tsx` lines 48-53: price `<span>` renders below label text with `block` display |
| AC2 | ✓ | Line 3 imports `CONTAINER_PRICES` from `cartCalculations.ts`; line 27 computes `formattedPrice` from it; no hardcoded values |
| AC3 | ✓ | Line 50: `className="block text-xs font-normal text-[var(--color-neutral-500)]"` matches spec |
| AC4 | ✓ | Line 8: `containerSize: SubkitSize` added to `EmptyContainerOptionProps`; additive, non-breaking |
| AC5 | ✓ | `ItemConfigScreen.tsx` line 213: `containerSize={currentSubkit?.size ?? 'regular'}`; `CustomSubkitScreen.tsx` line 242: `containerSize={customSubkit?.size ?? 'regular'}` |
| AC6 | ✓ | Parent components read subkit from store; prop updates reactively when size changes |
| AC7 | ✓ | All 8 existing render calls updated with `containerSize="regular"`; 2 new tests added (regular $40.00, large $60.00); axe tests include prop |
| AC8 | ✓ | `npm run typecheck` — zero errors; `npm run test:run` — 267/267 passed; `npm run lint` — no new lint errors |

### Improvements Checklist

No issues found. No items to address.

### Security Review

No security concerns. The change is a read-only import of a constant and a display-only price element. No user input is processed; no data flows to the server.

### Performance Considerations

No performance concerns. `CONTAINER_PRICES` is a static constant lookup. `formattedPrice` is computed inline with no memoization needed — it's a simple string concatenation on every render, which is negligible.

### Files Modified During Review

None. No refactoring was performed.

### Gate Status

Gate: PASS → docs/qa/gates/12.2-container-price-display.yml
Risk profile: Low — additive UI prop, no store/logic changes, no auth/payment/security files touched

### Recommended Status

✓ Ready for Done

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Initial story draft | Bob, SM Agent |
| 2026-03-11 | 1.1 | PO validation passed — approved (10/10 readiness, high confidence) | Sarah, PO Agent |

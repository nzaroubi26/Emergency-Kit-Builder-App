# Story 7.2: Clickable Visualizer Slot Navigation

## Status: Done

## Story

**As a** user,
**I want** to click on a filled slot in the housing unit visualizer,
**so that** I can jump directly to the item configuration screen for that subkit without stepping through the linear flow.

## Acceptance Criteria

1. `SubkitSelectionScreen` passes an `onSlotClick` handler to `HousingUnitVisualizer`. The handler navigates to `/configure/:subkitId` for the clicked subkit.
2. Filled slots display a `cursor-pointer` cursor and a `hover:brightness-95` visual affordance on hover.
3. Empty slots remain non-interactive — no cursor change, no click handler, no navigation.
4. Clicking a filled slot navigates to the correct `ItemConfigScreen`. The existing `subkitConfigGuard` validates the subkitId and allows navigation.
5. The `HousingUnitVisualizer` component interface is not changed — `onSlotClick?: (slotIndex: number) => void` was already defined in Phase 1.
6. `readOnly` mode on the Summary Page is unaffected — `onSlotClick` is not passed and slots remain non-interactive.

## Tasks / Subtasks

- [x] Task 1: Wire onSlotClick handler in SubkitSelectionScreen (AC: 1, 4)
  - [x] Add onSlotClick callback checking slot.status === 'filled'
  - [x] Navigate to /configure/:subkitId for the clicked subkit
  - [x] Pass handler to HousingUnitVisualizer component
- [x] Task 2: Add visual affordance to filled slots (AC: 2, 3)
  - [x] isInteractive logic in VisualizerSlot
  - [x] cursor-pointer on filled slots
  - [x] hover:brightness-95 on filled slots
  - [x] role="button" and aria-label for accessibility
- [x] Task 3: Ensure empty slots are non-interactive (AC: 3)
  - [x] No cursor change on empty slots
  - [x] No click handler on empty slots
  - [x] No navigation on empty slot click
- [x] Task 4: Verify Summary Page unaffected (AC: 6)
  - [x] readOnly mode does not pass onSlotClick
  - [x] Summary Page visualizer slots remain non-interactive

## Dev Notes

### Visualizer Slot Interactivity
[Source: docs/prd-phase2.md FR6, FR7]
- Filled slots: cursor-pointer + hover:brightness-95
- Empty slots: non-interactive, no click handler
- onSlotClick prop already defined in Phase 1 HousingUnitVisualizer interface

### Route Navigation
[Source: docs/architecture/7-routing.md]
- Navigate to `/configure/:subkitId` using React Router
- subkitConfigGuard validates the subkitId parameter

### Compatibility
[Source: docs/prd-phase2.md CR3, NFR8]
- HousingUnitVisualizer `onSlotClick` prop already typed — no interface changes
- readOnly mode on Summary Page unaffected

### Testing
[Source: docs/architecture/9-testing-requirements.md]
- All existing HousingUnitVisualizer.test.tsx tests pass without modification
- Verify filled slot click navigates correctly
- Verify empty slot click produces no navigation

## Dev Agent Record

### Files touched
- `src/components/subkit-selection/SubkitSelectionScreen.tsx` — onSlotClick handler checking slot.status === 'filled', navigates to /configure/:subkitId
- `src/components/visualizer/HousingUnitVisualizer.tsx` — propagates onSlotClick to VisualizerSlot
- `src/components/visualizer/VisualizerSlot.tsx` — isInteractive logic, hover:brightness-95, cursor-pointer, role="button", aria-label

## QA Results

Retroactively documented — implementation verified by PO assessment 2026-03-05

## Integration Verification

- IV1: Clicking a filled slot navigates to the correct Item Configuration screen for that subkit.
- IV2: Clicking an empty slot produces no navigation or console error.
- IV3: The Summary Page visualizer remains fully non-interactive.
- IV4: All existing `HousingUnitVisualizer.test.tsx` tests pass without modification.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-05 | 1.0 | Retroactive story creation — implementation complete | SM Agent |

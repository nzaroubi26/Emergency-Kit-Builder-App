# Story 14.4: Pets Subkit — Catalog Addition *(SUPERSEDED)*

## Status: Superseded by Story 15.1

> **SUPERSEDED.** This story has been replaced by Sprint 2 Story 15.1 (`docs/stories/15.1.story.md`), which includes updated acceptance criteria, Sally's color decisions (`#BE185D`/`#FFF1F2`), Winston's `STANDARD_CATEGORY_IDS` catch, and full item data with pricing.
>
> **Original context (preserved for reference):** This story was in PRD v1.0 but was deferred from Sprint 1 per Winston's architecture exclusions (Section 10). The MCQ data model captures `'pets'` as a `HouseholdOption` value in Sprint 1. The actual `CATEGORIES`/`ITEMS` data addition is Sprint 2 scope.

## Story

**As a** developer,
**I want** the Pets subkit added to the catalog with consistent data shape and minimum viable item content,
**so that** it is available as a selectable option and surfaced by MCQ-driven logic.

## Acceptance Criteria (Sprint 2)

1. New entry added to `CATEGORIES` in `kitItems.ts` with key `'pets'`. All existing shape fields populated. Color/tint and icon are James's implementation decision — must be visually distinct from existing 9 categories.
2. Minimum 3 items added to `ITEMS` with `categoryId: 'pets'`. All fields populated including `weightGrams` and `volumeIn3`. Suggested starting point:

| Suggested Name | weightGrams | volumeIn3 | Research Basis |
|---|---|---|---|
| Pet Food Supply | 1134 | 168 | 2.5lb dry kibble + 4 cans wet food; ~10"x7"x2.4" |
| Pet Water & Bowl Kit | 680 | 96 | Collapsible bowl + 1-gallon carrier; ~8"x4"x3" |
| Pet First Aid & Comfort Kit | 454 | 84 | Travel first aid + calming items in zip bag; ~7"x4"x3" |

3. Pets subkit is selectable in the "Build My Own" visualizer and surfaced by MCQ household composition logic.
4. `tsc --noEmit` passes. All existing tests pass.
5. Pets subkit renders correctly in the visualizer with no visual regressions.

## Tasks / Subtasks

> Tasks will be refined when this story is activated for Sprint 2.

- [ ] Task 1: Add Pets category to CATEGORIES (AC: 1)
  - [ ] Add `'pets'` entry to `CATEGORIES` in `src/data/kitItems.ts`
  - [ ] Assign visually distinct color/tint and icon
  - [ ] Populate all existing shape fields
- [ ] Task 2: Add Pets items to ITEMS (AC: 2)
  - [ ] Add minimum 3 items with `categoryId: 'pets'`
  - [ ] Populate all fields including `weightGrams`, `volumeIn3`, `pricePlaceholder`, `rating`, `reviewCount`
- [ ] Task 3: Wire MCQ surfacing logic (AC: 3)
  - [ ] Implement elevation rules: `householdComposition.includes('pets')` → elevate Pets subkit
  - [ ] Ensure Pets subkit appears in Build My Own visualizer
- [ ] Task 4: Verify TypeScript and tests (AC: 4, 5)
  - [ ] Run `tsc --noEmit` — zero errors
  - [ ] Run `npm run test:run` — all tests pass
  - [ ] Visual regression check on visualizer

## Dev Notes

### Sprint 1 Foundation
The MCQ data model (Story 13.1) captures `'pets'` as a valid `HouseholdOption` value. The store shape is Sprint 2 ready — no rework needed when this story is activated.

### Sprint 2 Dependencies
- MCQ → subkit surfacing logic (Sprint 2 scope)
- Visualizer UI refresh (Sprint 2 scope)
- This story can be worked in parallel with the visualizer refresh

### Item Data Research Basis
[Source: docs/prd-phase3/6-epic-details.md]

The suggested item weights and volumes are based on real-world product research:
- Pet Food Supply: 2.5lb dry kibble + 4 cans wet food; ~10"x7"x2.4"
- Pet Water & Bowl Kit: Collapsible bowl + 1-gallon carrier; ~8"x4"x3"
- Pet First Aid & Comfort Kit: Travel first aid + calming items in zip bag; ~7"x4"x3"

Final item content is a product owner decision — these are starting points.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-13 | 1.0 | Initial story draft (DEFERRED — Sprint 2) | Sarah, PO Agent |

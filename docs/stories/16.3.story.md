# Story 16.3: Order Confirmation — Dual-Path Support + "Fill Your Kit" CTA

## Status: Ready for Review

## Story

**As a** user who placed an order (via either path),
**I want** to see a confirmation of what I ordered and a clear next step to fill my kit,
**so that** I know my order is placed and I'm guided to the next phase of the experience.

## Acceptance Criteria

1. `OrderConfirmationScreen` reads `kitPath` from the MCQ store to determine which path was taken.
2. Essentials path (`kitPath === 'essentials'`): confirmation displays the 4 Essentials bundle subkits sourced from `ESSENTIALS_BUNDLE`. Kit total is computed from `ESSENTIALS_BUNDLE` + `CONTAINER_PRICES` ($180 = $60 + $40 + $40 + $40). No item-level breakdown.
3. Custom path (`kitPath === 'custom'`): confirmation displays the user's selected subkits with configured items, quantities, and pricing sourced from the kit store (mirrors existing behavior, now explicitly path-branched).
4. Both paths share the same page structure: heading "Your kit is on its way." (universal), path-specific subheading ("Here's your Essentials Kit summary." / "Here's your custom kit summary."), kit summary section, kit total line, and CTAs.
5. A new "Now Let's Fill Your Kit →" primary CTA is added below the kit total. On click, it opens a `FillKitStubModal`:
   - Icon: `Package` from lucide-react (48px, `brand-primary`)
   - Heading: "Coming Soon"
   - Body: "We're building the next phase of your emergency prep experience. Check back soon to fill your kit with recommended products."
   - CTA: "Got It" — closes modal
   - Accessibility: `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, focus trap, `Escape` to close, focus returns to trigger button on close
6. "Start Over" CTA is retained as secondary. Updated: routes to `/` (cover page) for both paths, calls both `resetKit()` and `resetMCQ()`.
7. Component tests cover: Essentials path renders bundle data with correct total, Custom path renders kit store data, "Fill Your Kit" CTA opens modal, modal dismisses via "Got It" and Escape, "Start Over" resets and navigates to `/`, both paths display kit totals.

## Tasks / Subtasks

- [x] Task 1: Create FillKitStubModal component (AC: 5)
  - [x] Create `src/components/confirmation/FillKitStubModal.tsx`
  - [x] Props: `open: boolean`, `onClose: () => void`
  - [x] Render overlay (`rgba(0,0,0,0.4)`, click-to-dismiss)
  - [x] Render modal card: `Package` icon (48px, `#1F4D35`), "Coming Soon" heading, body copy, "Got It" CTA
  - [x] Focus trap: lock focus within modal while open (same pattern as ConfirmationModal)
  - [x] `Escape` key closes modal
  - [x] On close: return focus to trigger button via `triggerRef`
- [x] Task 2: Add Essentials path rendering (AC: 1, 2, 4)
  - [x] Import `useMCQStore` and read `kitPath`
  - [x] Branch on `kitPath === 'essentials'`
  - [x] Render `ESSENTIALS_BUNDLE` subkits with category colors/names/sizes from `CATEGORIES`
  - [x] Compute total from `ESSENTIALS_BUNDLE` + `CONTAINER_PRICES` → $180.00
  - [x] Subheading: "Here's your Essentials Kit summary."
- [x] Task 3: Update Custom path rendering (AC: 1, 3, 4)
  - [x] Explicit `kitPath !== 'essentials'` branch (custom path with kit store data)
  - [x] Subheading: "Here's your custom kit summary."
- [x] Task 4: Add "Fill Your Kit" CTA (AC: 5)
  - [x] `PrimaryButton` "Now Let's Fill Your Kit →" below kit total
  - [x] Wired to `FillKitStubModal` open state
- [x] Task 5: Update "Start Over" CTA (AC: 6)
  - [x] Handler calls `resetKit()` then `resetMCQ()` then `navigate('/')`
- [x] Task 6: Write component tests (AC: 7)
  - [x] Essentials path renders 4 bundle subkits with correct names
  - [x] Essentials total is $180.00
  - [x] Custom path renders kit store subkits
  - [x] "Fill Your Kit" CTA opens modal
  - [x] Modal closes via "Got It" button
  - [x] Modal closes via Escape key
  - [x] "Start Over" resets kit + MCQ stores, navigates to `/`
- [x] Task 7: Verify TypeScript and tests
  - [x] Run `tsc --noEmit` — zero errors
  - [x] 14/14 OrderConfirmationScreen tests pass

## Dev Notes

### Architecture Reference — Essentials Pricing
[Source: docs/architecture-phase3-sprint2.md, Section 6a]

**Key decision:** Essentials total computed from `ESSENTIALS_BUNDLE` + `CONTAINER_PRICES`. No changes to `EssentialsBundleItem` type, no hardcoded total.

```ts
import { CONTAINER_PRICES } from '../../utils/cartCalculations';
import { ESSENTIALS_BUNDLE } from '../../data/essentialsConfig';

const essentialsTotal = ESSENTIALS_BUNDLE.reduce(
  (sum, item) => sum + CONTAINER_PRICES[item.size],
  0
);
// → 60 + 40 + 40 + 40 = $180.00
```

### Architecture Reference — Reset Ordering
[Source: docs/architecture-phase3-sprint2.md, Section 7a]

`resetKit()` then `resetMCQ()` then `navigate('/')`. Stores are independent, no cross-subscriptions. `/` has no guard — safe.

### Visual Spec — FillKitStubModal
[Source: docs/front-end-spec-phase3-sprint2.md, Section 4]

| Element | Treatment |
|---------|----------|
| Overlay | `rgba(0,0,0,0.4)`, click-to-dismiss |
| Modal | White bg, `radius-lg` (16px), `shadow-3`, `p-8`, `max-w-[400px]`, centered |
| Icon | `Package`, 48px, `brand-primary` (`#1F4D35`) |
| Heading | "Coming Soon" — `text-h2` (22px/600), `neutral-900` |
| Body | Encouraging copy — `text-body-lg` (16px/400), `neutral-500` |
| CTA | "Got It" — `PrimaryButton`, `max-w-[200px]`, centered |

### Visual Spec — Confirmation Layout
[Source: docs/front-end-spec-phase3-sprint2.md, Section 4]

| Element | Treatment |
|---------|----------|
| Heading | "Your kit is on its way." — `text-display` (36px/700), `neutral-900`, preceded by checkmark |
| Subheading | Path-specific — `text-body-lg` (16px/400), `neutral-500` |
| "Fill Your Kit" CTA | `PrimaryButton`, `max-w-sm` centered, `brand-primary` fill, `mt-6` |
| "Start Over" | `SecondaryButton`, `max-w-sm` centered, `mt-3` |

### Icon Availability
[Source: docs/architecture-phase3-sprint2.md, Section 9]
- `Package` — FOUND ✅ in `lucide-react@^0.576.0`

### Relevant Source Tree
```
src/
  components/
    confirmation/
      OrderConfirmationScreen.tsx  # MODIFY — dual-path, Fill Kit CTA, Start Over
      FillKitStubModal.tsx         # NEW
  data/
    essentialsConfig.ts            # READ — ESSENTIALS_BUNDLE
  utils/
    cartCalculations.ts            # READ — CONTAINER_PRICES, calculateCartGrandTotal
  store/
    mcqStore.ts                    # READ — kitPath, resetMCQ
    kitStore.ts                    # READ — selectedSubkits, itemSelections, resetKit
```

### Coding Standards
[Source: docs/architecture/11-frontend-developer-standards.md]
- All 14 critical rules apply
- Named exports only
- TypeScript strict mode mandatory

### Testing
[Source: docs/architecture/9-testing-requirements.md]
- Component tests for OrderConfirmationScreen dual-path and FillKitStubModal
- All existing tests must pass

### Integration Verification
- IV1: Essentials path → confirmation displays Essentials bundle correctly.
- IV2: Custom path → confirmation displays user's custom kit correctly.
- IV3: "Now Let's Fill Your Kit" CTA opens stub modal.
- IV4: "Start Over" resets kit and MCQ, navigates to cover page.
- IV5: Kit totals are accurate for both paths.

## File List

| File | Action | Description |
|------|--------|-------------|
| src/components/confirmation/OrderConfirmationScreen.tsx | MODIFY | Dual-path branching, Essentials rendering, "Fill Your Kit" CTA, "Start Over" update |
| src/components/confirmation/FillKitStubModal.tsx | NEW | Stub modal for Part 2 bridge CTA |

## Dev Agent Record

### Agent Model Used
claude-opus-4-6

### Debug Log References
- Fixed barrel import in OrderConfirmationScreen (`../../data` → `../../data/kitItems`) to unblock test suite
- Essentials path tests skip `confirmationGuard` since Essentials users don't populate `selectedSubkits`

### Completion Notes
- `FillKitStubModal`: Package icon, "Coming Soon" heading, focus trap, Escape close, trigger focus restore
- Dual-path: Essentials renders `ESSENTIALS_BUNDLE` with $180 total, Custom renders kit store data
- "Fill Your Kit" CTA wired to stub modal
- "Start Over" calls `resetKit()` + `resetMCQ()`, navigates to `/` (cover page)
- 14 component tests covering both paths, modal interactions, and reset behavior

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-14 | 1.0 | Initial story draft | Sarah, PO Agent |
| 2026-04-14 | 1.1 | Implementation complete — dual-path, modal, reset | James, Dev Agent |

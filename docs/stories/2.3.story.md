# Story 2.3: Slot Constraint Enforcement

## Status: Done

## Story

**As a** user,
**I want** the visualizer to prevent me from exceeding the 6-slot housing unit capacity,
**so that** my kit configuration always reflects a physically valid storage arrangement.

## Acceptance Criteria

1. The app tracks total slot usage in real time (Regular = 1 slot, Large = 2 slots)
2. When all 6 slots are occupied, remaining unselected subkit options are visually disabled
3. A clear visual indicator communicates when the housing unit is full
4. Switching a subkit from Regular to Large is blocked if insufficient slots remain — user is notified
5. Switching from Large to Regular correctly frees 1 slot and updates the visualizer immediately
6. Minimum slot constraint (3 subkits) is surfaced if user attempts to proceed with fewer selections

## Tasks / Subtasks

- [ ] Task 1: Implement slot usage tracking (AC: 1)
  - [ ] Use `useTotalSlotsUsed()` hook to display "N of 6 slots used" below visualizer
  - [ ] Update in real time as subkits are added/removed/resized
- [ ] Task 2: Create SlotFullIndicator component (AC: 3)
  - [ ] Create `src/components/visualizer/SlotFullIndicator.tsx`
  - [ ] Amber inline indicator displayed below visualizer when at capacity
  - [ ] Use `var(--color-status-warning)` color
  - [ ] Message: friendly, informational (e.g., "Your housing unit is full! Remove or resize a subkit to make room.")
  - [ ] ARIA: `announcePolite()` when capacity is reached
- [ ] Task 3: Implement disabled state for unselected cards (AC: 2)
  - [ ] When `useIsAtCapacity()` returns true, unselected subkit cards receive disabled visual state
  - [ ] Disabled state: `opacity-45 cursor-not-allowed` per UX spec
  - [ ] Transition to disabled state over 200ms
  - [ ] Cards must still be focusable for accessibility but non-interactive
- [ ] Task 4: Block invalid size changes (AC: 4)
  - [ ] `setSubkitSize` in store returns `false` when `canFitSize()` fails
  - [ ] Display inline amber message on SizeToggle when switching to Large is blocked
  - [ ] No modal — inline message only per UX spec
  - [ ] ARIA: announce blocked state
- [ ] Task 5: Handle size reduction (AC: 5)
  - [ ] Large → Regular: frees 1 slot immediately
  - [ ] Visualizer updates, previously disabled cards re-enable if capacity drops below 6
  - [ ] Slot below shifts up per fill order
- [ ] Task 6: Minimum subkit constraint messaging (AC: 6)
  - [ ] When fewer than 3 subkits selected, display friendly message near CTA
  - [ ] CTA uses `aria-disabled` + `aria-describedby` (NOT `disabled` attribute) per architecture Rule 6
  - [ ] Message: "Choose at least 3 categories to continue"

## Dev Notes

### Disabled State Specification
[Source: UX Spec Developer Implementation Notes]
- `opacity-45 cursor-not-allowed` for disabled subkit cards
- Transition to disabled state: 200ms
- Cards must remain focusable for keyboard users

### CTA Accessibility
[Source: docs/architecture/11-frontend-developer-standards.md, Rule 6]
- Configure Items CTA uses `aria-disabled` + `aria-describedby`, NOT the `disabled` attribute
- Keyboard users must be able to reach it and read the minimum message

### Slot Full Indicator
[Source: docs/architecture/3-project-structure.md]
- File: `src/components/visualizer/SlotFullIndicator.tsx`
- Amber inline indicator below visualizer
- Not a route — rendered inline on S1

### Store Constraints
[Source: docs/architecture/5-state-management.md#store]
- `selectSubkit()` checks `calculateTotalSlots(selectedSubkits) + 1 > MAX_SLOTS` before adding
- `setSubkitSize()` calls `canFitSize()` and returns boolean success/failure

### Testing

[Source: docs/architecture/9-testing-requirements.md]
- Test disabled state rendering and aria attributes
- Test capacity indicator appears at 6 slots
- Test size change blocking behavior
- Include axe assertions

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 1.0 | Initial story draft | Bob, SM Agent |
